# HTML 配色索引 · 给 AI 的参考

> 可直接独立复制的完整版本见同目录《全部Prompt_可直接复制.md》。该文件提供实际默认色值、字体、输入要求、交付和验收规则；浏览器复制版本会使用当前个人选择。新增表格、证据、文字层级、装饰、标注与组合见《组件索引_给AI.md》。

配色、文字与布局可组合选择。网页分「布局／配色与文字／图表」三个 Tab；文字区可单独编辑字体、加粗和高亮色。可将任意一组配色与《布局索引_给AI.md》中的任意布局组合；章节数量、文本长度与图表类型由任务决定。网页中的「复制布局与配色」会自动合并当前选择。

## 使用规则

- 只调整页面展示：背景、文字层级、边框、分隔线与留白。
- 完整保留内容、数据、单位、口径、来源与已有图表交互。既有图表的系列颜色含义、坐标轴、图例、visualMap、dataZoom 和回调不随页面主题批量覆盖。
- 新增图表按问题、数据关系与可辨识度独立选择图型和颜色。渐变是装饰，不能暗示没有依据的强弱、份额或频次。
- 强调色用于少量强调和装饰；长正文使用正文色。实际字号与背景仍需检查可读性。
- 没有真实数据时保留可审计空位，不为完成布局编造数字、引文或结论。

## 文字、重点与 Highlight

不要把「字体颜色」「重点色」「加粗」和「高亮底色」当作一个参数。九个角色独立，按当前色板保存；无自选设置时从色板生成默认值。

| 角色键 | 用途 | 默认来源 / 字重 |
|---|---|---|
| `body` | 连续阅读的正文 | `#273445` / 400 |
| `heading` | 页面大标题 H1 | 色板的 `heading` / 700 |
| `secondary` | 图注、来源与补充说明 | 色板的 `explain` / 400 |
| `emphasis` | 少量重点关键词 | 色板的 `accent`，必要时调亮度 / 500 |
| `strong` | 重要语句的加粗 | 色板的 `heading` / 700，允许与正文字色相同 |
| `highlightBg` | 短句或词的高亮底 | 强调原色混入 88% 白色；可另选 |
| `highlightText` | 高亮底上的文字 | 色板的 `heading`，按实际高亮底匹配 / 400 |
| `link` | 实际可点击的链接 | 色板的 `secondary`，保留下划线 / 400 |
| `subheading` | 章节与模块小标题 H2/H3 | 色板的 `explain` / 700 |

网页色块保留选色偏好，旁边显示实际使用色。普通文字以白底、页面底与该色板渐变端点为参考；高亮文字单独与高亮底比较。若不足 4.5:1，在黑／白方向逐步调整亮度，复制与导出记录调整后的实际色值。此规则只针对这些参考背景；换成图片、深色底或其他背景时要重新检查，不能宣称整页无条件达标。参考：[W3C 文字对比度](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)。

正文可从 16px、行高 1.75 起步。标题按内容层级另定大小，不通过缩小正文字号塞内容。`strong` 表示重要语句，`mark` 表示需要回看的标记；高亮与加粗可独立或组合使用。只标记原有、用户指定或有依据的重点，不借配色新增结论。参考：[MDN mark](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/mark)。

## 字体搭配

| 编号 | 方案 | 规则 |
|---|---|---|
| `system` | 清晰黑体 | 标题和正文使用设备系统无衬线字体；`PingFang SC`／`Microsoft YaHei` 等作为回退 |
| `editorial` | 宋体标题 | 标题采用 `Songti SC`／`STSong`／系统衬线回退，正文保持黑体 |
| `numeric` | 数字等宽 | 中文保持黑体，数字／编码使用 `ui-monospace`／`Menlo`／`Consolas` 等回退 |
| `qingnian` | 胡晓波青年宋 | 大小标题用 `HuXiaoBo-qingniansong`／`胡晓波青年宋`，正文与注释保留黑体；原生细款 300、粗款 700 |
| `freeland` | Freeland 英文标题 | 英文大标题使用 `Freeland` / 400，中文回退宋体；小标题宋体，正文与注释黑体 |

数字使用 `font-variant-numeric: tabular-nums`。字体依赖设备实际可用项，不能保证不同设备完全一致；页面不下载外部字体。完整回退序列在 HTML 的 `typography-data` 中，用户导出也包含当前字体序列。

字体和文字颜色按色板独立保存。收藏色板时连同其文字设置一起导出；当前布局、图表的 AI 描述也使用当前文字规范。恢复默认只重置当前色板的文字配置。这里的「文字配色」不重新定义图表系列色、色阶、类别映射或数据语义。

## 四层文字的独立参数

在「配色与文字」选择胡晓波青年宋或 Freeland，也可以让大标题、小标题、正文、注释各用不同字体、颜色、字号、字重、行高和段后距。

| 层级 | 默认字号 / 行高 | 默认段后距 | 手机规则 |
|---|---|---|---|
| 大标题 H1 | 40px / 1.25 | 24px | 最大 32px，长标题换行 |
| 小标题 H2 | 24px / 1.4 | 16px | 最大 26px，H3 可进一步分层 |
| 正文 | 16px / 1.75 | 18px | 不因内容多而缩小 |
| 注释 / 来源 | 14px / 1.65 | 12px | 保留完整来源和单位 |

一键字体组合会重设四层的字体与对应原生字重，保留已单独设置的字号和间距；「恢复这组默认」清除当前色板的文字自定项。整页工作台的文字层级预设提供基础值，显式自定的字号/行高/段距优先。Prompt 会列出实际值和哪些字段是用户自定，组件内有明确尺寸建议时，未自定项沿用组件建议。

胡晓波青年宋的字体家族名为 `HuXiaoBo-qingniansong`（细款 300、粗款 700）。Freeland 采用原生 400，适合英文标题，中文按宋体序列回退。网站不附带字体文件；同事电脑未安装时显示回退字体。四层配置随色板、收藏备份和保存搭配一起保留；旧备份可导入并按原字体方案补齐缺少的层级。

## 共用基础色

| 用途 | 色值 |
| --- | --- |
| 正文 | `#273445` |
| 页面底色 | `#fafbfc` |
| 内容面板 | `#ffffff` |
| 中性分隔线 | `#e8ebf0` |
| 解释文字 | `#4076a5` |

下列预置色值是色板的基础参考；有个性化设置时，以导出的九个文字角色实际色值为准。解释蓝沿用调整后的柔和蓝。标题与解释文字可加深以保证清晰，不直接使用高亮强调色承载长段正文。

## C01 谷雨粉蓝

粉蓝主渐变沿用谷雨原稿；适合封面、主视觉浅底与通用报告。

- 强调色：`#ec407a`；辅助色：`#4b91df`。
- 标题：`#ad1457`；解释文字：`#4076a5`。
- 主浅底：`linear-gradient(135deg, #fce4ec, #e3f2fd)`。
- 辅助浅底：`linear-gradient(135deg, #fff7fb, #eef7ff)`。
- 装饰细线：`linear-gradient(90deg, #db4c78, #a58a55)`，只作细线。

## C02 雾粉雾蓝

沿用原稿的空气感渐变；适合长文、多模块报告和大面积留白。

- 强调色：`#ec407a`；辅助色：`#4b91df`。
- 标题：`#ad1457`；解释文字：`#4076a5`。
- 主浅底：`linear-gradient(135deg, #fff7fb, #eef7ff)`。
- 辅助浅底：`linear-gradient(135deg, #fffafd, #f6fbff)`。
- 装饰细线：`linear-gradient(90deg, #ec407a, #4b91df)`。

## C03 柔粉留白

沿用原稿的粉色渐淡；适合观点区、章节扉页和浅底卡片。

- 强调色：`#ec407a`；辅助色：`#4b91df`。
- 标题：`#ad1457`；解释文字：`#4076a5`。
- 主浅底：`linear-gradient(180deg, #fff7fa, #ffffff)`。
- 辅助浅底：`linear-gradient(135deg, #fffafd, #ffffff)`。
- 装饰细线：`linear-gradient(90deg, #f48fb1, #ec407a)`。

## C04 柔蓝留白

沿用原稿的蓝色渐淡；适合证据区、研究方法和说明材料。

- 强调色：`#4b91df`；辅助色：`#ec407a`，少量使用。
- 标题与解释文字：`#4076a5`。
- 主浅底：`linear-gradient(180deg, #f5faff, #ffffff)`。
- 辅助浅底：`linear-gradient(135deg, #f6fbff, #ffffff)`。
- 装饰细线：`linear-gradient(90deg, #4b91df, #e3f2fd)`。

## C05 粉金细线

粉金细线沿用原稿；浅粉与暖白背景为重新搭配，不是原稿整套配色的直接复制。

- 强调色：`#db4c78`；辅助色：`#a58a55`。
- 标题：`#ad1457`；解释文字：`#4076a5`。
- 主浅底：`linear-gradient(135deg, #fff7fb, #fffef2)`。
- 辅助浅底：`linear-gradient(135deg, #fffafd, #ffffff)`。
- 装饰细线：`linear-gradient(90deg, #db4c78, #a58a55)`。
- 金色只少量点缀，粉金渐变用于细分隔线与短划线，不铺满正文背景。

## 自选色板

网页可保存名字、强调色、辅助色与渐变两端。自选渐变原色用作装饰；阅读浅底由每个渐变端点混入 88% 白色得到。标题从强调色、解释文字从辅助色逐步加深，使其与两端浅底的对比度达到至少 4.5:1。原色、实际文字色和浅底色都会写入可复制描述，供其他 AI 明确复用。

自选色板保存后自动收藏并预览，可以取消收藏或移除。收藏仅保存在当前浏览器，不在公开仓库或云端同步。导出的 Markdown 才包含访问者自己的色板与布局偏好；共享网站本身没有预选个人收藏。

## 机器可读入口

单文件 HTML 中的 JSON 数据块可直接读取，无需运行脚本：

- `script#layout-data`：32 种布局的编号、用途、描述、提醒和窄屏规则；新增布局还包含选择依据、加入日期及适用的官方参考链接。
- `script#palette-data`：5 组预设色板。`id` 是稳定标识，`code` 是沟通编号，`origin` 说明来源，`accent` / `secondary` 是强调与辅助色，`heading` / `explain` 是文字色，`start` / `end` / `angle` 定义主浅底渐变，`softStart` / `softEnd` 定义辅助浅底，`lineStart` / `lineEnd` 定义装饰细线，`usage` 定义使用范围。

- `script#typography-data`：9 个文字颜色角色、5 种字体搭配、4 层可编辑文字参数、字重、默认推导和作用域规则。个人设置按色板保存在浏览器 `shelley-html-typography-v1` 中，不在公开源码中。
- `script#chart-data`：40 种图表的编号、适用问题、字段、画法与边界。见《图表索引_给AI.md》。
- `script#chart-demo-data`：单独存放的人工示意值，全部标记 `is_demo: true`，不是业务输入。

切换配色只更新收藏册自己的布局、文字与图表示例，不修改其他图表实例。预览色会按辨识度适当加深，并保留数值、类别和方向含义。

布局与配色字段是页面展示规则，不是业务模板。只读取公开 HTML 无法得知某位访问者的个人收藏；请读取其导出清单。
