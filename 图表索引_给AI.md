# 图表索引｜给 AI 的独立使用规则

可独立复制且带完整默认配色、字体、输入与验收要求的版本见同目录《全部Prompt_可直接复制.md》；新增标注和组合见《组件索引_给AI.md》。浏览器复制使用当前个人设置。
本文件与 `HTML布局收藏册.html` 配套。40 种图表是可扩展的参考，不是默认图型或白名单；可与 32 种布局、已有或自选配色自由组合。

## 使用规则

1. 先确认问题、字段、粒度、单位、时间范围、分母与来源，再决定图型。用户收藏的图型仅表示偏好；不适合时说明原因并选更合适的图。
2. 页面内所有 `chart-demo-data` 均是人工构造的图型示意数据，`is_demo: true`。只用于看图形，不能复制进业务报告或当作真实证据。
3. 无真实数据时保留注明缺口的空位。不得补造指标、分数、区间、目标、阈值、流量、关系或结论。搜索、阅读、评论、交易没有可靠连接依据时不能拼成同一条链路。
4. 配色来自同目录《配色索引_给AI.md》或用户当前选择。渐变用于页面背景和装饰；图表颜色依类别、连续、正负等含义单独设定。已有图表的系列颜色、数据、坐标轴、图例、visualMap 和交互不得批量覆盖。
5. 同一量纲保持可比尺度。数值标签、颜色与形状至少提供可读的辨识方式；完整数据表可回查，不只依赖鼠标悬停。移动端仍须可读。
6. 本册只有自己的演示实例随色板变化。39 种使用内嵌 Apache ECharts 5.6.0，T40 为 SVG 词云。正式图表可使用合适的实现，不绑定某一引擎或示例配置。

## 读取 HTML 的方法

读取 `#layout-data` 获得布局元数据，`#palette-data` 获得预置配色，`#typography-data` 获得文字角色、字重与字体回退规则（实际个人值在导出清单中），`#chart-data` 获得每种图表的编号、名称、问题、字段、边界和用法。`#chart-demo-data` 是单独存放的演示值，不是业务输入。书签与自选色板存在浏览器中；跨 AI、跨设备复用时以导出的 Markdown 清单为准。

## 快速选图

| 要回答的问题 | 参考编号 |
|---|---|
| 比较 | T01 横向条形图、T02 纵向柱形图、T03 分组柱形图、T04 发散条形图、T05 棒棒糖图、T06 哑铃图、T07 坡度图、T08 子弹图 |
| 趋势 | T09 折线图、T10 阶梯折线图、T11 面积图、T12 堆叠面积图、T13 小多图、T14 排名变化图 |
| 构成 | T15 饼图、T16 环形图、T17 堆叠柱形图、T18 百分比堆叠条、T19 瀑布图、T20 帕累托图 |
| 分布 | T21 直方图、T22 箱线图、T23 点带分布图、T24 点与区间图 |
| 关系 | T25 散点图、T26 气泡图、T27 四象限图、T28 矩阵热力图、T29 雷达图、T39 平行坐标图 |
| 流向与层级 | T30 漏斗图、T31 桑基图、T32 关系网络图、T33 树状层级图、T34 矩形树图、T35 旭日图 |
| 时间与目标 | T36 日历热力图、T37 甘特图、T38 仪表图 |
| 文本 | T40 词云 |

## T01 横向条形图 · Horizontal bar

- 分类：比较
- 适合：比较类别大小，尤其是名称较长时
- 需要字段：类别、同口径数值
- 画法：按值排序，保留有业务含义的原顺序；标明单位。
- 使用边界：数量从零起画；类别重叠时不能解读为总体构成

给 AI 的描述：

请先检查真实数据是否满足「横向条形图」的字段与使用条件：类别、同口径数值。按值排序，保留有业务含义的原顺序；标明单位。 数量从零起画；类别重叠时不能解读为总体构成。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Bar](https://echarts.apache.org/handbook/en/how-to/chart-types/bar/basic-bar/)

## T02 纵向柱形图 · Column chart

- 分类：比较
- 适合：比较少量类别或离散期间
- 需要字段：类别或期间、数值
- 画法：每个柱子对应一个明确对象；保持零基线和一致尺度。
- 使用边界：不截断数量轴夸大差异；类别多或标签长时改横向

给 AI 的描述：

请先检查真实数据是否满足「纵向柱形图」的字段与使用条件：类别或期间、数值。每个柱子对应一个明确对象；保持零基线和一致尺度。 不截断数量轴夸大差异；类别多或标签长时改横向。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Bar](https://echarts.apache.org/handbook/en/how-to/chart-types/bar/basic-bar/)

## T03 分组柱形图 · Grouped bar

- 分类：比较
- 适合：同一维度内比较不同系列
- 需要字段：类别、系列、同口径数值
- 画法：系列并列且颜色映射稳定，保留各系列直接标签或图例。
- 使用边界：各组时期、范围与单位一致；系列过多改分面

给 AI 的描述：

请先检查真实数据是否满足「分组柱形图」的字段与使用条件：类别、系列、同口径数值。系列并列且颜色映射稳定，保留各系列直接标签或图例。 各组时期、范围与单位一致；系列过多改分面。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Bar](https://echarts.apache.org/handbook/en/how-to/chart-types/bar/basic-bar/)

## T04 发散条形图 · Diverging bar

- 分类：比较
- 适合：比较相对零点的正负差值
- 需要字段：类别、有符号差值、单位
- 画法：显示零线、正负号和实际单位，明确相对什么基准变化。
- 使用边界：差值与同比不可混称；没有参照点不能人为设定正负

给 AI 的描述：

请先检查真实数据是否满足「发散条形图」的字段与使用条件：类别、有符号差值、单位。显示零线、正负号和实际单位，明确相对什么基准变化。 差值与同比不可混称；没有参照点不能人为设定正负。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T05 棒棒糖图 · Lollipop chart

- 分类：比较
- 适合：用轻量图形比较少量类别
- 需要字段：类别、同口径数值
- 画法：圆点位置编码数值，细线连到共同零基线，保证标签可读。
- 使用边界：圆点大小不承载额外含义；类别太多应改普通条形

给 AI 的描述：

请先检查真实数据是否满足「棒棒糖图」的字段与使用条件：类别、同口径数值。圆点位置编码数值，细线连到共同零基线，保证标签可读。 圆点大小不承载额外含义；类别太多应改普通条形。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T06 哑铃图 · Dumbbell chart

- 分类：比较
- 适合：比较同一对象的两个可比状态
- 需要字段：对象、状态甲数值、状态乙数值
- 画法：每行一个对象，用两端点与连接线展示前后或两组差异。
- 使用边界：两个点须来自同一对象与一致口径，连线本身不证明因果

给 AI 的描述：

请先检查真实数据是否满足「哑铃图」的字段与使用条件：对象、状态甲数值、状态乙数值。每行一个对象，用两端点与连接线展示前后或两组差异。 两个点须来自同一对象与一致口径，连线本身不证明因果。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T07 坡度图 · Slope chart

- 分类：比较
- 适合：看两个离散时点之间谁升谁降
- 需要字段：对象、两个可比时点、数值
- 画法：仅连接已知端点并显示两端值，保持共同尺度，避免把连线称为连续趋势。
- 使用边界：仅两点不能说明中间轨迹；线的倾斜角受尺度影响

给 AI 的描述：

请先检查真实数据是否满足「坡度图」的字段与使用条件：对象、两个可比时点、数值。仅连接已知端点并显示两端值，保持共同尺度，避免把连线称为连续趋势。 仅两点不能说明中间轨迹；线的倾斜角受尺度影响。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T08 子弹图 · Bullet chart

- 分类：比较
- 适合：看实际值相对目标的位置
- 需要字段：对象、实际值、目标值、范围
- 画法：用短条表示实际值、垂直标记表示目标，背景范围仅使用已定义界限。
- 使用边界：目标与背景区间须有明确来源；不能凭视觉填出好坏档位

给 AI 的描述：

请先检查真实数据是否满足「子弹图」的字段与使用条件：对象、实际值、目标值、范围。用短条表示实际值、垂直标记表示目标，背景范围仅使用已定义界限。 目标与背景区间须有明确来源；不能凭视觉填出好坏档位。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T09 折线图 · Line chart

- 分类：趋势
- 适合：看有序时间上的变化
- 需要字段：有序时间、数值、可选系列
- 画法：时间按真实间距排列，标出首末期间、单位与缺失值处理。
- 使用边界：不连接不同口径，不把缺失期当零，不随意平滑制造峰谷

给 AI 的描述：

请先检查真实数据是否满足「折线图」的字段与使用条件：有序时间、数值、可选系列。时间按真实间距排列，标出首末期间、单位与缺失值处理。 不连接不同口径，不把缺失期当零，不随意平滑制造峰谷。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T10 阶梯折线图 · Step line

- 分类：趋势
- 适合：看在某时刻生效并持续的状态
- 需要字段：变更时点、状态值、生效规则
- 画法：按实际生效时点绘制水平保持段与跳变，并说明采用左连续或右连续。
- 使用边界：明确变更在区间起点还是终点生效，不把连续变量硬画成台阶

给 AI 的描述：

请先检查真实数据是否满足「阶梯折线图」的字段与使用条件：变更时点、状态值、生效规则。按实际生效时点绘制水平保持段与跳变，并说明采用左连续或右连续。 明确变更在区间起点还是终点生效，不把连续变量硬画成台阶。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T11 面积图 · Area chart

- 分类：趋势
- 适合：强调单一序列随时间的量级
- 需要字段：有序时间、非负可加数值
- 画法：在单一折线下填浅色，保留零基线与清晰边界，不用渐变暗示额外变量。
- 使用边界：面积从零起画；重叠面积不适合精确比较多个系列

给 AI 的描述：

请先检查真实数据是否满足「面积图」的字段与使用条件：有序时间、非负可加数值。在单一折线下填浅色，保留零基线与清晰边界，不用渐变暗示额外变量。 面积从零起画；重叠面积不适合精确比较多个系列。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T12 堆叠面积图 · Stacked area

- 分类：趋势
- 适合：看总体及其组成随时间的变化
- 需要字段：时间、互斥成分、非负可加数值
- 画法：固定系列顺序和颜色，展示成分值及总量；比较各自走势时改多条折线。
- 使用边界：成分须同分母且能相加；中间层不能直接按顶部高度读值

给 AI 的描述：

请先检查真实数据是否满足「堆叠面积图」的字段与使用条件：时间、互斥成分、非负可加数值。固定系列顺序和颜色，展示成分值及总量；比较各自走势时改多条折线。 成分须同分母且能相加；中间层不能直接按顶部高度读值。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Line](https://echarts.apache.org/handbook/en/how-to/chart-types/line/stacked-line/)

## T13 小多图 · Small multiples

- 分类：趋势
- 适合：在统一坐标下比较多个系列
- 需要字段：时间、系列、同单位数值
- 画法：各分面共用单位、时间窗、轴范围和类别顺序，不压缩字号。
- 使用边界：比较量级时所有小图同尺度；若使用独立尺度需醒目标明

给 AI 的描述：

请先检查真实数据是否满足「小多图」的字段与使用条件：时间、系列、同单位数值。各分面共用单位、时间窗、轴范围和类别顺序，不压缩字号。 比较量级时所有小图同尺度；若使用独立尺度需醒目标明。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T14 排名变化图 · Bump chart

- 分类：趋势
- 适合：比较对象的名次随时间变化
- 需要字段：时间、对象、排名、并列规则
- 画法：纵轴用数值名次并将第一名置顶，端点标明对象，不将名次落差解释为规模差。
- 使用边界：排名不代表数值差距；补齐并列、退出和新增对象的处理规则

给 AI 的描述：

请先检查真实数据是否满足「排名变化图」的字段与使用条件：时间、对象、排名、并列规则。纵轴用数值名次并将第一名置顶，端点标明对象，不将名次落差解释为规模差。 排名不代表数值差距；补齐并列、退出和新增对象的处理规则。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T15 饼图 · Pie chart

- 分类：构成
- 适合：粗看少量互斥部分占整体的比例
- 需要字段：类别、非负可加数值、统一总量
- 画法：说明分母与总体，展示类别和比例，不使用三维或爆炸效果。
- 使用边界：类别必须互斥且穷尽或明确剩余；差异很小时改条形

给 AI 的描述：

请先检查真实数据是否满足「饼图」的字段与使用条件：类别、非负可加数值、统一总量。说明分母与总体，展示类别和比例，不使用三维或爆炸效果。 类别必须互斥且穷尽或明确剩余；差异很小时改条形。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Pie](https://echarts.apache.org/handbook/en/how-to/chart-types/pie/doughnut/)

## T16 环形图 · Donut chart

- 分类：构成
- 适合：展示少量构成并补充整体说明
- 需要字段：类别、数值、统一总量
- 画法：环片按真实占比绘制，中心只放有依据的总量或说明，保留类别与比例。
- 使用边界：中间文字不能替代分母；类别多或需精确比较时改条形

给 AI 的描述：

请先检查真实数据是否满足「环形图」的字段与使用条件：类别、数值、统一总量。环片按真实占比绘制，中心只放有依据的总量或说明，保留类别与比例。 中间文字不能替代分母；类别多或需精确比较时改条形。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Pie](https://echarts.apache.org/handbook/en/how-to/chart-types/pie/doughnut/)

## T17 堆叠柱形图 · Stacked bar

- 分类：构成
- 适合：比较多组总量和内部组成
- 需要字段：组别、互斥成分、非负可加数值
- 画法：组内堆叠、组间保留共同零基线，工具提示列出各成分及总量。
- 使用边界：成分和总量必须可加；不同分母的百分比不能直接堆叠

给 AI 的描述：

请先检查真实数据是否满足「堆叠柱形图」的字段与使用条件：组别、互斥成分、非负可加数值。组内堆叠、组间保留共同零基线，工具提示列出各成分及总量。 成分和总量必须可加；不同分母的百分比不能直接堆叠。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T18 百分比堆叠条 · 100% stacked bar

- 分类：构成
- 适合：比较不同组的构成比例
- 需要字段：组别、成分分子、组内分母
- 画法：按组内真实分母归一到 100%，显示比例与分母，明确四舍五入误差。
- 使用边界：总量不同的组归一化后不能比较规模；必须保留各组样本量

给 AI 的描述：

请先检查真实数据是否满足「百分比堆叠条」的字段与使用条件：组别、成分分子、组内分母。按组内真实分母归一到 100%，显示比例与分母，明确四舍五入误差。 总量不同的组归一化后不能比较规模；必须保留各组样本量。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T19 瀑布图 · Waterfall

- 分类：构成
- 适合：解释起点到终点的可加变化
- 需要字段：起点、有符号变动项、终点
- 画法：用起终点柱与增减浮动柱构成桥接，显示符号并校验起点加变动等于终点。
- 使用边界：所有变动项必须与终点对账，不能把相关因素当作可加贡献

给 AI 的描述：

请先检查真实数据是否满足「瀑布图」的字段与使用条件：起点、有符号变动项、终点。用起终点柱与增减浮动柱构成桥接，显示符号并校验起点加变动等于终点。 所有变动项必须与终点对账，不能把相关因素当作可加贡献。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T20 帕累托图 · Pareto chart

- 分类：构成
- 适合：看少数类别贡献了多少累计总量
- 需要字段：互斥类别、非负可加数值
- 画法：按数值降序排列柱形，累计曲线从同一数组计算，区分原值轴与百分比轴。
- 使用边界：累计百分比须由同一排序与分母计算；不强行套用二八法则

给 AI 的描述：

请先检查真实数据是否满足「帕累托图」的字段与使用条件：互斥类别、非负可加数值。按数值降序排列柱形，累计曲线从同一数组计算，区分原值轴与百分比轴。 累计百分比须由同一排序与分母计算；不强行套用二八法则。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T21 直方图 · Histogram

- 分类：分布
- 适合：看连续变量的分布形状
- 需要字段：逐条数值、分箱边界、样本量
- 画法：使用明确且不重叠的数值分箱，相邻箱紧接；标出频数或密度及样本量。
- 使用边界：箱宽会影响外观；不等宽箱用密度，边界开闭与缺失处理须明确

给 AI 的描述：

请先检查真实数据是否满足「直方图」的字段与使用条件：逐条数值、分箱边界、样本量。使用明确且不重叠的数值分箱，相邻箱紧接；标出频数或密度及样本量。 箱宽会影响外观；不等宽箱用密度，边界开闭与缺失处理须明确。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[NIST · Histogram](https://www.itl.nist.gov/div898/handbook/eda/section3/histogra.htm)

## T22 箱线图 · Box plot

- 分类：分布
- 适合：比较不同组的中位数与离散程度
- 需要字段：组别、原始观测或经核验的分位统计
- 画法：从真实观测计算分位数，写明须线与异常点定义，不仅展示均值。
- 使用边界：必须说明四分位数算法和须线规则；须端不一定是最大最小值

给 AI 的描述：

请先检查真实数据是否满足「箱线图」的字段与使用条件：组别、原始观测或经核验的分位统计。从真实观测计算分位数，写明须线与异常点定义，不仅展示均值。 必须说明四分位数算法和须线规则；须端不一定是最大最小值。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[NIST · Box plot](https://www.itl.nist.gov/div898/handbook/eda/section3/boxplot.htm)

## T23 点带分布图 · Strip plot

- 分类：分布
- 适合：在样本较少时保留每个观测点
- 需要字段：稳定观测 ID、组别、数值
- 画法：每个点对应一条记录，沿数值轴定位；分组方向的微小错位不承载数值。
- 使用边界：抖动只用于防重叠，不代表第二个变量；过多样本要控制遮挡

给 AI 的描述：

请先检查真实数据是否满足「点带分布图」的字段与使用条件：稳定观测 ID、组别、数值。每个点对应一条记录，沿数值轴定位；分组方向的微小错位不承载数值。 抖动只用于防重叠，不代表第二个变量；过多样本要控制遮挡。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T24 点与区间图 · Point and interval

- 分类：分布
- 适合：同时展示估计与已知上下界
- 需要字段：对象、估计值、下界、上界、区间含义
- 画法：用点表示估计、线段表示真实区间，标明区间类型、方法及必要样本信息。
- 使用边界：无估计方法和置信水平不能称置信区间；区间重叠不等于差异检验

给 AI 的描述：

请先检查真实数据是否满足「点与区间图」的字段与使用条件：对象、估计值、下界、上界、区间含义。用点表示估计、线段表示真实区间，标明区间类型、方法及必要样本信息。 无估计方法和置信水平不能称置信区间；区间重叠不等于差异检验。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T25 散点图 · Scatter plot

- 分类：关系
- 适合：看两个数值变量的关系
- 需要字段：观测 ID、数值 X、数值 Y、同一粒度
- 画法：明确两轴含义、单位与观测粒度，每个点可回查来源，保留缺失与异常处理。
- 使用边界：相关不代表因果；不要混合总体和明细，注意分母共用造成的关系

给 AI 的描述：

请先检查真实数据是否满足「散点图」的字段与使用条件：观测 ID、数值 X、数值 Y、同一粒度。明确两轴含义、单位与观测粒度，每个点可回查来源，保留缺失与异常处理。 相关不代表因果；不要混合总体和明细，注意分母共用造成的关系。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Scatter](https://echarts.apache.org/handbook/en/how-to/chart-types/scatter/basic-scatter/)

## T26 气泡图 · Bubble chart

- 分类：关系
- 适合：在散点上加入第三个量级
- 需要字段：观测 ID、X、Y、非负大小值
- 画法：位置表示 X/Y，气泡面积与第三变量成比例，并说明大小含义与图例。
- 使用边界：用面积而非半径正比编码大小；重叠与极大气泡会掩盖对象

给 AI 的描述：

请先检查真实数据是否满足「气泡图」的字段与使用条件：观测 ID、X、Y、非负大小值。位置表示 X/Y，气泡面积与第三变量成比例，并说明大小含义与图例。 用面积而非半径正比编码大小；重叠与极大气泡会掩盖对象。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

官方参考：[Apache ECharts · Scatter](https://echarts.apache.org/handbook/en/how-to/chart-types/scatter/basic-scatter/)

## T27 四象限图 · Quadrant scatter

- 分类：关系
- 适合：按两个有业务含义的阈值分区
- 需要字段：对象、X、Y、有来源的横纵阈值
- 画法：使用真实量化轴和已说明依据的阈值线，保留点位数值与边界对象。
- 使用边界：阈值不能为了好看设在中点；象限名称不自动证明战略价值

给 AI 的描述：

请先检查真实数据是否满足「四象限图」的字段与使用条件：对象、X、Y、有来源的横纵阈值。使用真实量化轴和已说明依据的阈值线，保留点位数值与边界对象。 阈值不能为了好看设在中点；象限名称不自动证明战略价值。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T28 矩阵热力图 · Heatmap

- 分类：关系
- 适合：找两维组合中的高低与稀疏
- 需要字段：行维度、列维度、数值、覆盖规则
- 画法：每格对应一个有明确口径的组合，固定色阶并提供图例、数值与缺失说明。
- 使用边界：零与缺失分开；顺序量用连续色阶，正负量才考虑分歧色阶

给 AI 的描述：

请先检查真实数据是否满足「矩阵热力图」的字段与使用条件：行维度、列维度、数值、覆盖规则。每格对应一个有明确口径的组合，固定色阶并提供图例、数值与缺失说明。 零与缺失分开；顺序量用连续色阶，正负量才考虑分歧色阶。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T29 雷达图 · Radar chart

- 分类：关系
- 适合：概览少量对象的可比多维轮廓
- 需要字段：对象、维度、同量纲或冻结标准化后的值
- 画法：所有维度明确量表、方向和范围，保留原始数值及标准化方法，限制对象数量。
- 使用边界：不能凭印象打分；维度顺序影响形状，面积不宜用来排名

给 AI 的描述：

请先检查真实数据是否满足「雷达图」的字段与使用条件：对象、维度、同量纲或冻结标准化后的值。所有维度明确量表、方向和范围，保留原始数值及标准化方法，限制对象数量。 不能凭印象打分；维度顺序影响形状，面积不宜用来排名。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T30 漏斗图 · Funnel

- 分类：流向与层级
- 适合：展示同一批对象在有序阶段中的保留
- 需要字段：同一队列、有序阶段、去重人数或一致计数
- 画法：核验队列、事件、时间窗与分母后再绘制阶段量；证据不足时改独立指标而非漏斗。
- 使用边界：搜索、阅读、购买等独立证据链不能拼接；重复进入须有处理口径

给 AI 的描述：

请先检查真实数据是否满足「漏斗图」的字段与使用条件：同一队列、有序阶段、去重人数或一致计数。核验队列、事件、时间窗与分母后再绘制阶段量；证据不足时改独立指标而非漏斗。 搜索、阅读、购买等独立证据链不能拼接；重复进入须有处理口径。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T31 桑基图 · Sankey diagram

- 分类：流向与层级
- 适合：展示可核验的量在节点间如何分配
- 需要字段：来源节点、目标节点、非负流量、连接依据
- 画法：线宽编码真实流量，节点与连接可回查，并解释损耗、遗漏或流量不守恒原因。
- 使用边界：线宽必须有流量依据；共现不等于迁移，内部节点需要核对收支

给 AI 的描述：

请先检查真实数据是否满足「桑基图」的字段与使用条件：来源节点、目标节点、非负流量、连接依据。线宽编码真实流量，节点与连接可回查，并解释损耗、遗漏或流量不守恒原因。 线宽必须有流量依据；共现不等于迁移，内部节点需要核对收支。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T32 关系网络图 · Network graph

- 分类：流向与层级
- 适合：查看有证据支持的对象连接
- 需要字段：节点 ID、边的两端、关系类型、可选权重
- 画法：明确每条边的业务含义及有向性，保留节点与边的来源，布局只服务阅读。
- 使用边界：距离与中心位置不自动代表重要性；箭头和因果含义须有依据

给 AI 的描述：

请先检查真实数据是否满足「关系网络图」的字段与使用条件：节点 ID、边的两端、关系类型、可选权重。明确每条边的业务含义及有向性，保留节点与边的来源，布局只服务阅读。 距离与中心位置不自动代表重要性；箭头和因果含义须有依据。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T33 树状层级图 · Tree diagram

- 分类：流向与层级
- 适合：展示明确的父子归属
- 需要字段：唯一节点 ID、父节点 ID、名称
- 画法：按已定义分类树展开父子关系，保证节点可定位，长标签与分支可读。
- 使用边界：一个节点多父级时不再是简单树；层级不等于因果

给 AI 的描述：

请先检查真实数据是否满足「树状层级图」的字段与使用条件：唯一节点 ID、父节点 ID、名称。按已定义分类树展开父子关系，保证节点可定位，长标签与分支可读。 一个节点多父级时不再是简单树；层级不等于因果。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T34 矩形树图 · Treemap

- 分类：流向与层级
- 适合：同时查看层级归属与可加规模
- 需要字段：父子层级、叶子非负数值、汇总规则
- 画法：矩形面积编码叶子规模，颜色用于有限类别区分，提供层级路径、数值与总量。
- 使用边界：面积难做精确比较；父值要与子项对账，不能混用单位

给 AI 的描述：

请先检查真实数据是否满足「矩形树图」的字段与使用条件：父子层级、叶子非负数值、汇总规则。矩形面积编码叶子规模，颜色用于有限类别区分，提供层级路径、数值与总量。 面积难做精确比较；父值要与子项对账，不能混用单位。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T35 旭日图 · Sunburst

- 分类：流向与层级
- 适合：从中心向外阅读分层构成
- 需要字段：父子层级、叶子数值、统一总量
- 画法：使用真实层级与可加值，解释扇区角度的构成含义，保留路径、数值和返回入口。
- 使用边界：外圈面积受半径影响，不能把外圈更大理解为贡献更高

给 AI 的描述：

请先检查真实数据是否满足「旭日图」的字段与使用条件：父子层级、叶子数值、统一总量。使用真实层级与可加值，解释扇区角度的构成含义，保留路径、数值和返回入口。 外圈面积受半径影响，不能把外圈更大理解为贡献更高。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T36 日历热力图 · Calendar heatmap

- 分类：时间与目标
- 适合：观察逐日节奏与周期
- 需要字段：真实日期、每日指标、覆盖状态
- 画法：以真实日历定位日期，固定指标与色阶，分别标注零值和缺失，并保留日期查询。
- 使用边界：没有记录不一定为零；不跨口径或时区直接比较，不能制造完整日历

给 AI 的描述：

请先检查真实数据是否满足「日历热力图」的字段与使用条件：真实日期、每日指标、覆盖状态。以真实日历定位日期，固定指标与色阶，分别标注零值和缺失，并保留日期查询。 没有记录不一定为零；不跨口径或时区直接比较，不能制造完整日历。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T37 甘特图 · Gantt chart

- 分类：时间与目标
- 适合：看事项起止与时间上的重叠
- 需要字段：事项、开始时间、结束时间、可选状态
- 画法：条形长度对应真实持续时间，明确计划或实际、起止日期及边界约定。
- 使用边界：计划与实际区分，标明结束边界；时间条不能自动推导依赖或完成度

给 AI 的描述：

请先检查真实数据是否满足「甘特图」的字段与使用条件：事项、开始时间、结束时间、可选状态。条形长度对应真实持续时间，明确计划或实际、起止日期及边界约定。 计划与实际区分，标明结束边界；时间条不能自动推导依赖或完成度。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T38 仪表图 · Gauge

- 分类：时间与目标
- 适合：查看单一指标在固定范围中的位置
- 需要字段：指标值、下限、上限、目标或阈值依据
- 画法：说明量程与指标含义，只显示有依据的目标和阈值；需精确比较多个对象时用条形。
- 使用边界：范围不能随值随意改变；装饰色带不等于实际风险或好坏

给 AI 的描述：

请先检查真实数据是否满足「仪表图」的字段与使用条件：指标值、下限、上限、目标或阈值依据。说明量程与指标含义，只显示有依据的目标和阈值；需精确比较多个对象时用条形。 范围不能随值随意改变；装饰色带不等于实际风险或好坏。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T39 平行坐标图 · Parallel coordinates

- 分类：关系
- 适合：查看多个观测在多维数值上的组合
- 需要字段：观测 ID、多维数值、各维量纲与方向
- 画法：每条线对应一个对象，逐轴标出含义与范围，保持标准化方法和高低方向可解释。
- 使用边界：轴顺序会改变视觉关系；标准化、反向轴及筛选规则须写清

给 AI 的描述：

请先检查真实数据是否满足「平行坐标图」的字段与使用条件：观测 ID、多维数值、各维量纲与方向。每条线对应一个对象，逐轴标出含义与范围，保持标准化方法和高低方向可解释。 轴顺序会改变视觉关系；标准化、反向轴及筛选规则须写清。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## T40 词云 · Word cloud

- 分类：文本
- 适合：快速浏览词语与相对出现频次
- 需要字段：词语、真实频数、统计单位、清洗与去重规则
- 画法：依据真实文本统计词频并保留分词、同义词合并、停用词和计数单位；字号只辅助浏览，附精确频数表。
- 使用边界：不能用词云判断需求份额、情感或因果；精确比较频次请用条形或表格

给 AI 的描述：

请先检查真实数据是否满足「词云」的字段与使用条件：词语、真实频数、统计单位、清洗与去重规则。依据真实文本统计词频并保留分词、同义词合并、停用词和计数单位；字号只辅助浏览，附精确频数表。 不能用词云判断需求份额、情感或因果；精确比较频次请用条形或表格。若条件不足，说明缺口或另选合适图型。仅使用真实数据，不复制本册示意值；保留单位、时间、来源、分母、缺失说明和交互。配色按用户选择及本文件通用规则执行，收藏此图不限制未来图型。

## 演示数据口径补充

所有演示值、来源标记与特殊假设均可在对应卡片的「查看示意数据」回查。直方图公开分箱边界；箱线使用 `(n−1)p` 线性插值分位数，须端为最小/最大值；点与区间图的上下界不是置信区间；气泡按面积表达大小；四象限界线、目标值与仪表量程只是教学设定；漏斗仅在虚构的同一示意队列中成立；桑基与层级示意数值可加且守恒；甘特图结束日不含；词云词频是人为设定，不来自真实语料。

## 引擎与可访问性参考

- [Apache ECharts 图表尺寸与重绘](https://echarts.apache.org/handbook/en/concepts/chart-size/)
- [Apache ECharts 可访问性](https://echarts.apache.org/handbook/en/best-practices/aria/)
