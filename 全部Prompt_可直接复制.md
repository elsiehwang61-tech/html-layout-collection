# HTML 收藏册 · 全部可复制 Prompt

共 193 项。每段可独立复制，连同本次正文、真实数据或现有 HTML 一起发送。
编号只是检索名，具体规则、色值与字体已写全；无需其他聊天。备选样式按内容选择，不要把互斥的整页结构或色板同时套用。个人浏览器导出的版本带当前设置，公开文件使用默认样式。

## 01 杂志式 · 布局

【任务】请按以下「01 杂志式布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以杂志式主从排版组织正文：主标题与核心论述占主要空间，关键引文或发现放在较窄的辅助区域，证据与行动含义按需接续。
适合：长篇洞察、研究报告、章节开篇。使用提醒：重要信息太多时应拆段；不要把所有句子都放大。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏依照标题、正文、辅助材料的顺序单列阅读。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 02 主图侧注 · 布局

【任务】请按以下「02 主图侧注布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用较宽的主区域呈现本次合适的图表、图片或表格，旁边安排观察、解释与行动含义，来源与口径紧邻证据。
适合：图表解读、数据分析、证据展示。使用提醒：侧注过长应放到图下；不能为排版压缩坐标轴或删掉口径。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏先呈现主证据，再按顺序显示侧注；宽表在自身区域滚动。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 03 Bento 大小卡片 · 布局

【任务】请按以下「03 Bento 大小卡片布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
采用大小有别的卡片网格，将首要结论放在大区域，辅助证据与行动建议放在较小区域；按内容需要确定模块数和顺序。
适合：摘要页、机会盘点、独立短模块。使用提醒：不适合把连续长文切成碎块；卡片尺寸必须随内容增长。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏优先显示主结论，其余内容按逻辑顺序堆叠。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 04 左右对照 · 布局

【任务】请按以下「04 左右对照布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将两个比较对象并列，按相同维度对齐呈现主张、证据、限制和适用条件，在下方汇总共同点、分歧与选择依据。
适合：方案、人群、场景或产品表达比较。使用提醒：两侧必须使用可比维度；不能凭视觉对称补造资料。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏可按维度依次呈现两侧内容，始终保留对象名称。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 05 纵向叙事 · 布局

【任务】请按以下「05 纵向叙事布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
沿纵向组织问题、证据、解释和行动等实际段落，适度错开位置制造节奏；段落数由内容决定，保留每一步的依据与边界。
适合：案例复盘、策略推导、问题到行动。使用提醒：视觉顺序不代表因果，段落关系必须有内容依据。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏使用顺畅单列，减少左右错位和多余留白。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 06 原声拼贴 · 布局

【任务】请按以下「06 原声拼贴布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将已核验的原声按主题组织成大小有别的引文区，突出代表性表达，保留完整语境、来源与不同看法，再给出有边界的归纳。
适合：评论、访谈、定性洞察、共识与分歧。使用提醒：不能用卡片面积暗示频次；不同观点与反例也要保留。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏单列呈现原声，不截断长引文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 07 居中长文 · 布局

【任务】请按以下「07 居中长文布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
使用居中的适宜阅读宽度承载左对齐正文，以清晰标题、段间距和必要引用组织长文；大图与宽表可以在需要时扩展。
适合：深度文章、研究方法、完整论证。使用提醒：长文不必全做居中文字；正文应左对齐，宽图可局部扩展。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏保留舒适边距，不缩小字号硬塞。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 08 分屏封面 · 布局

【任务】请按以下「08 分屏封面布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
开篇用文字与主视觉分屏：一侧呈现主题、一句判断与必要背景，另一侧放与主题直接相关的真实图片或有明确含义的视觉。
适合：报告开篇、品牌专题、场景故事。使用提醒：必须有值得展示的图像；图不应抢走标题或承载正文。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏按标题、说明、主视觉堆叠，确保首屏能读懂主题。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 09 全幅开场 · 布局

【任务】请按以下「09 全幅开场布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用全幅主视觉构成开场，将短标题与一句引言安排在清晰区域；长正文、图表和方法说明放到后续独立阅读区域。
适合：专题封面、章节情境、品牌故事。使用提醒：图像上文字要清晰；正文和关键数值不应压在杂乱背景上。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏调整图片裁切与文字位置，保留主体和可读对比。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 10 侧栏索引 · 布局

【任务】请按以下「10 侧栏索引布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
在桌面保留简洁章节索引，正文占主要宽度，目录锚点与当前章节一致；章节数与模块数按实际内容生成。
适合：多章节报告、资料手册、可反复查阅内容。使用提醒：窄屏不能一直挤占正文；目录层级要对应真实章节。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏将目录收为可打开的菜单，正文使用完整可用宽度。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 11 报纸多栏 · 布局

【任务】请按以下「11 报纸多栏布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将相对独立的议题组织为报纸式多栏，使用统一标题层级与细分隔线；明确每栏的起止和阅读顺序，宽证据按需跨栏。
适合：行业简报、并列议题、信息速读。使用提醒：连续论述不宜被打乱阅读顺序；多列表格仍应独占足够宽度。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏把各议题按逻辑顺序转为单列，避免细窄文字栏。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 12 卷轴叙事 · 布局

【任务】请按以下「12 卷轴叙事布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以卷轴式叙事讲解同一命题：在足够宽的屏幕中保留问题或主视觉，另一侧顺序展示证据、解释和结论；滚动始终由读者控制。
适合：一个命题的逐层讲解、深度专题。使用提醒：不应劫持滚动；没有逐层关系的内容无需强加动画。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏改为自然文档顺序；不依赖固定定位或动画才能读完内容。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 13 主图画廊 · 布局

【任务】请按以下「13 主图画廊布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以一张主图带领一组相关图片，主图和缩略图均附必要的主题与来源；按材料关系安排顺序，允许查看清晰完整内容。
适合：案例图片、内容素材、视觉方向对照。使用提醒：缩略图不能代替关键证据的清晰原图，来源要能回查。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏顺序显示图片与说明，保留图片原比例和必要细节。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 14 Z 形图文 · 布局

【任务】请按以下「14 Z 形图文布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将一组组相关图文左右交替组织，每组只表达一个重点；组内保持判断、材料与解释的紧密关系，组数由内容决定。
适合：多个场景、产品亮点、案例章节。使用提醒：不为交替而拆散证据，文字长度和图片比例应允许变化。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏每组按照标题、图片、说明的固定逻辑顺序展示。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 15 不对称留白 · 布局

【任务】请按以下「15 不对称留白布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
围绕一条重点判断采用不对称留白，将标题、主视觉和短说明错开但保持明确阅读顺序；必要证据可以延伸到下一模块。
适合：主题页、关键判断、精简提案。使用提醒：信息量大时应增加页面或模块，不能靠大留白隐藏必要解释。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏减少装饰性空白，内容自然流动，避免绝对定位遮挡。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 16 大图叠文 · 布局

【任务】请按以下「16 大图叠文布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
让简短观点卡与真实场景图轻微交叠，建立图文关联，同时完整保留图片主体和文字；后续证据独立展开。
适合：场景洞察、品牌故事、案例开篇。使用提醒：不要遮住图像证据、文字或主体；长文应回到正常流式排版。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏取消重叠，将图片和说明按顺序堆叠。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 17 正文边注 · 布局

【任务】请按以下「17 正文边注布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用连贯正文承载主论证，在相关段落旁添加短边注，放术语、来源和必要补充；关键证据仍留在主要阅读路径中。
适合：研究方法、复杂论证、来源与术语解释。使用提醒：决定结论的证据不能藏在边注里，边注字号也要可读。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏将边注紧跟对应段落显示，并保留对应关系。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 18 跨栏观点 · 布局

【任务】请按以下「18 跨栏观点布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
在长文适当位置加入跨栏观点区，突出已经有证据支撑的关键句或准确引用，再回到正文继续论述；观点区不替代完整证据。
适合：长文中的核心判断、章节小结。使用提醒：只突出少数关键句；引用与作者自己的归纳要分清。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏让观点自然占据一行容器，不强制文字只排一行。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 19 瀑布流 · 布局

【任务】请按以下「19 瀑布流布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用瀑布流组织相互独立、长度不同的材料，保持每条材料完整并标注来源；不把排列位置或面积作为排名、份额或频次。
适合：灵感素材、图片库、独立案例与原声。使用提醒：不适合严格排序或逐行比较，卡片面积不代表重要性或频次。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏使用单列，保留可理解的文档阅读顺序。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 20 横向清单 · 布局

【任务】请按以下「20 横向清单布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用横向清单呈现独立条目，每项包含必要缩略图、清楚标题、短摘要及来源或行动；列表顺序依据任务需要明确安排。
适合：案例目录、资源清单、行动项。使用提醒：摘要过长时应展开详情，列表不应只剩相似的大段文字。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏保留标题与摘要优先，图片可缩小或移至上方。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 21 矩阵看板 · 布局

【任务】请按以下「21 矩阵看板布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
按两个有明确含义的维度构成内容矩阵，每格放对应观点与材料；保留行列标题及空白原因，不编造强弱或量化着色。
适合：场景 × 议题、人群 × 内容、多个对象对照。使用提醒：这不是自动生成的热力图；格子里没有数据时不能用深浅编造强弱。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏保留矩阵语义，可在独立区域横向滚动或转成带行列标签的分组清单。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 22 中心辐射 · 布局

【任务】请按以下「22 中心辐射布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以一个核心命题为中心，周围放必要的并列维度，使用明确标签说明关系；连线只有在关系有依据时添加，不把布局关系当成因果证明。
适合：核心命题与并列维度、框架总览。使用提醒：位置与连线不能暗示未经证实的因果；维度多时应改分组清单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏以核心命题在上、相关维度在下的顺序显示。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 23 时间轴 · 布局

【任务】请按以下「23 时间轴布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
按已核实的时间或阶段组织事件，每项保留日期、事实、材料与必要解释，清楚区分历史、当前与计划。
适合：项目进展、案例历程、事件演变。使用提醒：时间先后不代表因果；没有日期或阶段依据时不补造时间。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏使用纵向单列时间轴，不压缩事件正文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 24 章节扉页 · 布局

【任务】请按以下「24 章节扉页布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
在重要章节之间加入克制的扉页，以章节编号、主题和一句导语建立过渡；扉页数量与高度按报告篇幅决定。
适合：长报告换章、汇报节奏、重要主题过渡。使用提醒：章节短或很多时无需每章占满一屏，避免空白拖长报告。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏收紧留白，让下一段内容容易进入视线。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 25 决策摘要页 · 布局

【任务】请按以下「25 决策摘要页布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以决策摘要页组织内容：开头点明本次要决定的问题和有依据的判断，接续必要证据与适用边界，末尾给出明确行动或待决事项，并提供完整材料入口；模块数量由内容决定。
适合：方案开篇、老板速读、会议决策。使用提醒：只选最重要的信息做摘要，并链接完整证据；不为凑一屏删去决定结论的限制。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏按判断、证据、限制、行动单列展开，长内容自然增高，不压缩成固定一屏。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 26 案例主从浏览 · 布局

【任务】请按以下「26 案例主从浏览布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
采用案例主从浏览：列表显示必要标题、短摘要和标签，主区域呈现当前选中案例的完整材料、判断与来源；清楚标示当前选择，支持键盘操作、可回查链接和返回列表。检索和标签只按实际材料生成。
适合：竞品案例库、素材库、多个案例深读。使用提醒：保持当前选中项、键盘焦点和返回位置；需要同时对照多个对象时使用比较布局。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏改为列表到详情的自然导航，提供返回入口并保留列表位置；打印与无脚本阅读时保留全部必要内容。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 27 多方案对比 · 布局

【任务】请按以下「27 多方案对比布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将多个备选方案按统一维度并列比较，完整展示适用条件、支撑依据、投入与限制，长字段允许扩展；只有评价规则和证据充分时才标注推荐，不用装饰面积或虚构分数制造高低。
适合：多个策略方案、产品卖点、资源组合选择。使用提醒：没有统一评价标准时，不标最佳、不编造打分；方案过多或字段过长时改用完整对照表。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏转为带维度标签的方案分组，或在独立对照表内横向滚动；保留对象和维度对应关系。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 28 图像标注 · 布局

【任务】请按以下「28 图像标注布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
保留完整且清晰的原图，用少量有编号的标记指向需要解释的真实位置，旁侧或图下按编号提供说明与来源；可以查看原图，标记不能遮住关键细节，静态版也能读懂对应关系。
适合：笔记拆解、产品图解、页面与包装分析。使用提醒：标注不能遮挡原图证据；不以装饰连线暗示因果，原图与标注内容需可区分。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏把说明移到图下并保留编号，标记支持触摸与键盘，不依赖悬停才能看到必要信息。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 29 页签分组 · 布局

【任务】请按以下「29 页签分组布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以简短清晰的页签切换同一主题下相互独立的材料；保留公共标题、关键结论与当前选择，支持键盘、焦点和明确的选中状态。所有内容保留在文档中，打印与无脚本模式可完整阅读。图表面板显示后按容器更新尺寸，保留已有实例、数据和交互。
适合：同一主题下独立的案例、人群或材料视图。使用提醒：需要同时比较或必须顺序读完的信息不放在互斥页签里，核心结论始终可见。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏可改成带标题的顺序分组，或保留可触达的页签；不能裁切标签、挤压图表或隐藏关键解释。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 30 折叠详解 · 布局

【任务】请按以下「30 折叠详解布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
将可按需查阅的补充资料按清楚标题组织为折叠区，核心判断、必要证据和重要边界常驻可见。优先使用原生 details/summary，允许键盘操作和多个区块同时展开；为打印、导出与无脚本阅读保留完整内容，图表展开后正确适配尺寸。
适合：方法补充、口径说明、常见疑问与附录。使用提醒：读者必须掌握的结论、重要限制和行动要求直接展示；材料本身很短时无需折叠。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏用整行可点击标题与清晰展开状态，展开内容自然增高；不把长文塞进固定高度滚动窗。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 31 字段档案 · 布局

【任务】请按以下「31 字段档案布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
为单个对象建立清晰档案：顶部说明对象名称与范围，下方以语义化 dl/dt/dd 组织字段及其值，相关来源就近保留；字段顺序按查阅需要安排，长值自然换行，缺失与未验证项如实标注。
适合：产品档案、案例信息、研究样本与证据来源卡。使用提醒：字段来自实际资料，未知项明确标注；多对象量化比较应使用表格，不以排版补齐缺失事实。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏可将字段名放在对应值上方，保持逐项关联；长链接和文本能换行。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 32 行动看板 · 布局

【任务】请按以下「32 行动看板布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
按项目实际状态建立行动看板，每张卡片保留具体动作、已有负责人、已确认时间和完成依据；未知项如实标注，顺序与状态来自真实记录。若需要交互移动，提供键盘操作或状态菜单，并明确保存结果，不只依赖拖拽。
适合：策略落地、项目推进、实验与任务跟踪。使用提醒：卡片位置由真实状态决定；卡片数量和面积不等于进度或完成率，不擅自改变任务状态。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
窄屏按状态分组纵向展示，保持状态标题与卡片关联；任务正文完整可读，打印时所有阶段展开。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 33 期刊卷首 · 布局

【任务】请按以下「33 期刊卷首布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
以最大 1120px 居中的期刊卷首组织页面。顶部为期号或分类与 1px 细线，中部单一大标题和 120 字内已有摘要，下面按 2:1 排列正文起始与章节索引。标题 44–64px，正文 16–18px；期号缺失时省略，不编造。留白 48–72px，后续长文回到舒适的 680–760px 行宽。
适合：专题开篇、品牌观点、长篇报告。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 34 作品档案 · 布局

【任务】请按以下「34 作品档案布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
最大宽 1200px，使用 minmax(180px,0.7fr) minmax(0,2.3fr) 网格，间距 40px。左侧显示材料中已有的名称、分类、日期与来源，右侧依次显示主题主视觉、摘要、正文和材料。档案栏仅在高度不超过视口时 sticky，不能挡住正文；缺字段明确待提供。
适合：案例档案、作品集、专题合集。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 35 章节色带 · 布局

【任务】请按以下「35 章节色带布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
每章用整行浅色带作为标题区，左侧小型章节号，右侧 H2 和一句已有导语。章头内边距 24–32px，章节之间 56px，正文白底最大 760px；必要图表可扩展到 1120px。色带按当前色板装饰渐变，正文与图表的颜色语义保持独立。
适合：方法手册、培训资料、结构化报告。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 36 研究札记 · 布局

【任务】请按以下「36 研究札记布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
采用最大 1040px 的研究札记布局，正文 2fr、注释 0.8fr，列距 32px。标题下给日期、主题和出处留轻量文本行；主文保持 16–18px/1.8，边注 14px/1.65。细线与浅色标签只辅助定位，不加妨碍文字的横线纹理。边注按段落编号对应，重要信息仍在主文呈现。
适合：观察记录、读书笔记、轻量研究。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 37 分幕演示页 · 布局

【任务】请按以下「37 分幕演示页布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
按主题分为若干 section，每幕用章节小标签、一个 H2、一段已有核心论述和主视觉或证据。桌面可使用 min-height:60svh 形成节奏，高度由内容自然增长；禁止固定高度裁剪或强制滚动吸附。幕间浅底与白底交替，末尾提供下一章锚点。长图和完整来源不被折叠隐藏。
适合：概念说明、提案演示、连续主题。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 38 专题索引展签 · 布局

【任务】请按以下「38 专题索引展签布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
顶部显示专题名和完整导读，下方使用 3 列展签卡片：小编号、标题、简短摘要、真实章节锚点。最大宽 1120px，卡片间距 20px，卡片内边距 24px；可按文字或图片材料改变卡片高度，不能用省略号替代完整标题。后续章节按与目录一致的顺序展示。
适合：专题入口、知识合集、灵感档案。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 39 错位图文 · 布局

【任务】请按以下「39 错位图文布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
桌面两列按 1.15:1 排列，图像列从顶部开始，文字列用 margin-top:40px 形成错位；列距 40px。标题、正文、来源组成独立文本块，图片保持原比例。错位使用正常文档流，禁止负边距遮挡；下一组可反转视觉位置但 DOM 仍按阅读逻辑排列。
适合：产品故事、情绪专题、品牌介绍。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 40 主图与编号注解 · 布局

【任务】请按以下「40 主图与编号注解布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
桌面主图占 62%，旁边 38% 列出编号注解。标记只放在材料可确认的位置；不清楚坐标时将编号放在图下清单，不能凭空指认。图片按原比例 contain；来源紧随图像。标号用文字而非只用颜色，正文按编号顺序排列，图上不覆盖原有证据文字。
适合：包装拆解、结构解读、细节说明。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 41 叙事分镜 · 布局

【任务】请按以下「41 叙事分镜布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
按真实材料数量使用重复 figure，桌面 3 列、平板 2 列、手机 1 列，每项由原比例图片、顺序号、标题与正文构成。列距 24px，图下间距 12px；帧数由内容决定。只有确认时间顺序时写先后，不能将独立样本编成同一人的经历。
适合：步骤演示、内容故事、使用场景。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 42 并置批注 · 布局

【任务】请按以下「42 并置批注布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
两列等宽，每列都保留图片完整比例、真实名称、采集条件与来源。底部另设全宽批注带，按相同维度分条写差异和边界。只有相同采集条件时才讨论可比差异；不把不同角度、光线或修图当成效果证据。手机按对象一、对象二、共同比较说明排序。
适合：方案视觉对照、包装对比、案例拆解。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 43 全图摘要带 · 布局

【任务】请按以下「43 全图摘要带布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用 figure 放完整主图，图下接浅底 figcaption 摘要带。摘要带桌面分为标题 1fr 与说明 2fr，内边距 24px；来源另起 14px 文本行。图片不叠字、不遮挡，若是截图应可查看原尺寸。没有图片时省略主图区域，按正常文字模块呈现。
适合：案例头图、活动视觉、产品介绍。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 44 引语与画面 · 布局

【任务】请按以下「44 引语与画面布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
桌面 45% 引语区与 55% 图片区并列，引语使用 blockquote，字号 24–32px；署名和来源仅使用提供内容。图片保持原比例，下面独立标明分析与推测。过长引语改用正常字号完整呈现，不随意改写为更有冲击力的金句。
适合：访谈摘录、案例观察、人物表达。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 45 编号索引清单 · 布局

【任务】请按以下「45 编号索引清单布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
使用 ol，每项按 56px 编号列和 minmax(0,1fr) 内容列排列。顺序号 32px，标题 20px，正文 16px，项间 24px。序号表示阅读顺序；没有真实排名时不使用 TOP 或奖牌。可加真实锚点，按钮只出现于实际可执行动作。
适合：方法要点、执行清单、知识索引。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 46 页签档案卡 · 布局

【任务】请按以下「46 页签档案卡布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
每张卡片由小分类页签和正文面板组成，页签正常文档流贴近面板顶部，圆角 10px；面板使用细边框与 24px 内边距。桌面 2–3 列，卡片随内容增高；分类标签与标题都保留真实文字，不把纯装饰页签做成假按钮。
适合：概念说明、资料分组、产品档案。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 47 主卡与侧列 · 布局

【任务】请按以下「47 主卡与侧列布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
外层网格 1.6fr 1fr，主卡占左侧，辅助卡在右侧垂直排列。间距 20px，卡片内边距 24px。主卡承接用户指定主信息，辅助卡保留补证与延伸；不能把字号、面积或位置当作统计权重。手机按主卡及辅助材料顺序排列。
适合：核心观点加补充、主案例与相关材料。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 48 证据条目台账 · 布局

【任务】请按以下「48 证据条目台账布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
使用语义列表或 table 展示来源条目；每项含材料名、摘要、证据状态、来源入口。标题 18px，说明 16px，状态文字 14px；行间只保留细线。颜色只辅助状态标签，缺来源直接注明。若含表格必须保留表头、完整行列与局部滚动，不把条目数当去重人数。
适合：资料清单、来源核对、研究材料。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 49 场景分区拼板 · 布局

【任务】请按以下「49 场景分区拼板布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
桌面以三列网格放场景区块，每块有小标签、H3、已有场景描述与素材位。可以一块顶部放图、一块底部放图，以图文顺序建立节奏；DOM 保留每块完整归属。区块面积默认相当；未经数据验证，不通过更大面积暗示人群或需求占比。
适合：场景清单、概念梳理、内容选题。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## 50 章节入口卡 · 布局

【任务】请按以下「50 章节入口卡布局」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
卡片采用真实 a 链接并可键盘聚焦，每张包含栏目小字、完整标题、简短说明与方向箭头；最小点击高度 44px。网格使用 repeat(auto-fit,minmax(min(100%,240px),1fr))，留白 24px。目标不存在时展示“链接待提供”的静态卡片，禁止使用空 href 或无效果按钮。
适合：章节导航、延伸阅读、知识主页。使用提醒：结构按材料增减；序号仅表示阅读顺序，不是排名。保留完整内容、图片说明与证据来源。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C01 谷雨粉蓝 · 配色

【任务】请按以下「C01 谷雨粉蓝配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C02 雾粉雾蓝 · 配色

【任务】请按以下「C02 雾粉雾蓝配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C02 雾粉雾蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fff7fb, #eef7ff)；更浅的辅助底 linear-gradient(135deg, #fffafd, #f6fbff)；装饰细线 linear-gradient(90deg, #ec407a, #4b91df)。更浅的粉蓝用于长文与多模块报告，保持大面积留白；强调色少量用于编号和短标签。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3f75a3。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3f75a3。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #cb3769 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #3b73b0 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C03 柔粉留白 · 配色

【任务】请按以下「C03 柔粉留白配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C03 柔粉留白」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(180deg, #fff7fa, #ffffff)；更浅的辅助底 linear-gradient(135deg, #fffafd, #ffffff)；装饰细线 linear-gradient(90deg, #f48fb1, #ec407a)。柔粉向白色渐淡，用于观点区、扉页和浅底卡片；解释文字仍用柔和蓝，正文用深灰。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #4076a5。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #4076a5。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #cd386a / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #3d75b5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C04 柔蓝留白 · 配色

【任务】请按以下「C04 柔蓝留白配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C04 柔蓝留白」。装饰强调色 #4b91df，辅助色 #ec407a；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(180deg, #f5faff, #ffffff)；更浅的辅助底 linear-gradient(135deg, #f6fbff, #ffffff)；装饰细线 linear-gradient(90deg, #4b91df, #e3f2fd)。柔蓝向白色渐淡，用于证据区和方法说明；解释蓝为 #4076a5，不用原先较深的蓝色；粉色少量辅助。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #4076a5。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #4076a5。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #4076a5。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #3d75b5 / 字重 500；加粗字色 #4076a5 / 字重 700；Highlight 底色 #e9f2fb / 字重 400；Highlight 字色 #3e72a0 / 字重 400；链接字色 #cd386a / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C05 粉金细线 · 配色

【任务】请按以下「C05 粉金细线配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C05 粉金细线」。装饰强调色 #db4c78，辅助色 #a58a55；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fff7fb, #fffef2)；更浅的辅助底 linear-gradient(135deg, #fffafd, #ffffff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉金渐变只用于细分隔线、短划线；浅粉与暖白背景为本次组合。金色少量点缀，不铺满正文背景。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #4076a5。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #4076a5。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #c5446c / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fbeaef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #867045 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C06 墨绿香槟 · 配色

【任务】请按以下「C06 墨绿香槟配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C06 墨绿香槟」。装饰强调色 #2f483d，辅助色 #b29a71；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #edf1e9, #f6eedf)；更浅的辅助底 linear-gradient(135deg, #f6f8f3, #fcf9f2)；装饰细线 linear-gradient(90deg, #2f483d, #b29a71)。墨绿原色 #2f483d 用于大标题、加粗与少量装饰；香槟金 #b29a71 只点缀细线和编号，正文不用浅金。鼠尾草浅底过渡到燕麦奶白，避免大面积墨绿底；Highlight 使用暖香槟浅底配墨绿字。
原色 #2f483d：大标题、重点与装饰强调。气质：沉静 · 温润 · 自然。适合：品牌提案、成分故事、品质感封面。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #2f483d。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #526449。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #303d36。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #626c62。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #79603f / 字重 500；加粗字色 #2f483d / 字重 700；Highlight 底色 #efe5d1 / 字重 400；Highlight 字色 #2f483d / 字重 400；链接字色 #42675c / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C07 薄荷云白 · 配色

【任务】请按以下「C07 薄荷云白配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C07 薄荷云白」。装饰强调色 #386655，辅助色 #b29caa；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #d6f5e3, #f3edf4)；更浅的辅助底 linear-gradient(135deg, #f1fbf5, #faf6fb)；装饰细线 linear-gradient(90deg, #74aa95, #b29caa)。薄荷绿原色 #d6f5e3 用于主浅底和 Highlight 底，保留清透感；深苔绿负责标题和重点，灰紫用于小标题和链接。灰玫瑰只作小面积辅助与细线。浅薄荷不作白底文字或细小图表线条。
原色 #d6f5e3：浅底渐变与 Highlight 底色。气质：清透 · 轻盈 · 柔和。适合：护肤资料、夏日专题、清爽长文。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #305344。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #626078。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #34413a。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #646e66。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #426657 / 字重 500；加粗字色 #305344 / 字重 700；Highlight 底色 #d6f5e3 / 字重 400；Highlight 字色 #305344 / 字重 400；链接字色 #63577b / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C08 摩卡亚麻 · 配色

【任务】请按以下「C08 摩卡亚麻配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C08 摩卡亚麻」。装饰强调色 #8e745c，辅助色 #8b947d；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f2ece5, #f8f6ef)；更浅的辅助底 linear-gradient(135deg, #faf7f3, #fbfaf5)；装饰细线 linear-gradient(90deg, #8e745c, #a7ac95)。摩卡原色 #8e745c 用于装饰强调与细线，搭配亚麻米白、灰鼠尾草；可读文字另用深咖和灰橄榄。Highlight 是浅燕麦底配深咖字。大面积保持浅底，避免整页变成深棕底或统一棕色正文。
原色 #8e745c：装饰强调、细线与色块。气质：克制 · 细腻 · 暖调。适合：编辑式报告、品牌档案、产品说明。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #634b38。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #62644f。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #40382f。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #726a60。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #79614c / 字重 500；加粗字色 #634b38 / 字重 700；Highlight 底色 #e8decd / 字重 400；Highlight 字色 #584637 / 字重 400；链接字色 #5b6651 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C09 奶油黄橄榄 · 配色

【任务】请按以下「C09 奶油黄橄榄配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C09 奶油黄橄榄」。装饰强调色 #646c3f，辅助色 #a7a0b7；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f3e692, #faf6df)；更浅的辅助底 linear-gradient(135deg, #fbf7de, #f8f5f9)；装饰细线 linear-gradient(90deg, #c5b86a, #a49bab)。奶油黄原色 #f3e692 用于封面浅底与 Highlight，深橄榄负责大标题和加粗，灰紫少量用于解释与辅助。黄色不作白底细字，不表示预警或增长等数据语义；需要警示时单独设计状态标签。
原色 #f3e692：主浅底与 Highlight 底色。气质：明亮 · 舒展 · 有分寸。适合：灵感手册、活动提案、生活方式封面。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #454f30。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #635c73。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #393d31。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #686453。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #596136 / 字重 500；加粗字色 #454f30 / 字重 700；Highlight 底色 #f3e692 / 字重 400；Highlight 字色 #454f30 / 字重 400；链接字色 #625371 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C10 酒红玫瑰 · 配色

【任务】请按以下「C10 酒红玫瑰配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C10 酒红玫瑰」。装饰强调色 #713b50，辅助色 #b18e7b；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f4e7ec, #f4ede3)；更浅的辅助底 linear-gradient(135deg, #fcf5f8, #faf7f1)；装饰细线 linear-gradient(90deg, #713b50, #b18e7b)。酒红用于标题、加粗和关键句，玫瑰粉过渡至香槟米色作为浅底；暖金棕仅用于短细线和小标签。Highlight 配深酒红字，正文保持暖深灰。避免红色铺满页面或自动作为负向指标。
原色 #713b50：标题与装饰强调。气质：馥郁 · 柔润 · 精致。适合：品牌故事、女性主题、质感封面。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #632f44。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #765d56。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #43363c。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #73666a。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #803d58 / 字重 500；加粗字色 #632f44 / 字重 700；Highlight 底色 #f0dce5 / 字重 400；Highlight 字色 #632f44 / 字重 400；链接字色 #805267 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C11 雾蓝银灰 · 配色

【任务】请按以下「C11 雾蓝银灰配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C11 雾蓝银灰」。装饰强调色 #59778b，辅助色 #a3aaa6；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #e6eef2, #f2f1eb)；更浅的辅助底 linear-gradient(135deg, #f4f8fa, #faf9f5)；装饰细线 linear-gradient(90deg, #59778b, #a3aaa6)。雾蓝用于编号、强调和装饰线，深灰蓝用于标题；雾蓝浅底过渡到珍珠灰，辅助采用灰绿。Highlight 用浅蓝底，正文保留深灰蓝。解释文字采用中等深度的灰蓝，实际亮度以可读性检查为准。
原色 #59778b：装饰强调与结构线。气质：理性 · 通透 · 安静。适合：研究简报、方法说明、信息型长文。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #39576c。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #526c76。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #31424d。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #5f6d74。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #506f83 / 字重 500；加粗字色 #39576c / 字重 700；Highlight 底色 #dae8ef / 字重 400；Highlight 字色 #39576c / 字重 400；链接字色 #446979 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C12 灰紫燕麦 · 配色

【任务】请按以下「C12 灰紫燕麦配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C12 灰紫燕麦」。装饰强调色 #786a86，辅助色 #aa9680；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #ede9f1, #f4ede1)；更浅的辅助底 linear-gradient(135deg, #f8f5fa, #fbf8f2)；装饰细线 linear-gradient(90deg, #786a86, #aa9680)。灰紫负责标题与重点氛围，燕麦色提供温暖辅助，主浅底由雾紫过渡到米杏。文字使用深灰紫与深咖层级；Highlight 保持淡紫底与深紫字。灰紫不自动作为图表某类或分数的固定编码。
原色 #786a86：装饰强调与细线。气质：柔雅 · 知性 · 松弛。适合：趋势灵感、编辑专题、创意方案。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #5a4c68。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #74614f。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #393442。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #6f6774。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #705b82 / 字重 500；加粗字色 #5a4c68 / 字重 700；Highlight 底色 #e8e0ef / 字重 400；Highlight 字色 #544460 / 字重 400；链接字色 #615770 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C13 陶土米杏 · 配色

【任务】请按以下「C13 陶土米杏配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C13 陶土米杏」。装饰强调色 #a66950，辅助色 #7e8967；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f4e4d8, #f8f1e5)；更浅的辅助底 linear-gradient(135deg, #fcf6f0, #fbf8f1)；装饰细线 linear-gradient(90deg, #a66950, #7e8967)。陶土橘用于装饰强调、编号和短细线，深陶棕用于标题，灰橄榄用于辅助。米杏渐变只作浅底；Highlight 用暖杏底配深棕字。保持暖中性色正文，不能将陶土色自动解释为上涨、危险或损失。
原色 #a66950：装饰强调与细线。气质：温暖 · 朴素 · 有质感。适合：植物主题、生活方式、产品手册。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #774b37。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #626b50。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #453c33。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #70665c。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #8a523d / 字重 500；加粗字色 #774b37 / 字重 700；Highlight 底色 #efdcc8 / 字重 400；Highlight 字色 #70452f / 字重 400；链接字色 #5b6749 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C14 橄榄灰粉 · 配色

【任务】请按以下「C14 橄榄灰粉配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C14 橄榄灰粉」。装饰强调色 #414c39，辅助色 #c7a19b；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #eee8da, #c7a19b)；更浅的辅助底 linear-gradient(135deg, #f7f5ef, #f1eee4)；装饰细线 linear-gradient(90deg, #9ea98d, #896d59)。暖象牙作主阅读底，深橄榄建立标题层次；灰玫瑰与鼠尾草保留为可见色块，陶棕点缀细线。封面可用暖象牙到灰玫瑰的渐变，正文保持浅底。原色不统一混白；Highlight 用暖象牙底配深橄榄字。
原色色卡：深橄榄 #414c39（大标题与结构色）；鼠尾草 #9ea98d（辅助色块）；暖象牙 #eee8da（主阅读底）；灰玫瑰 #c7a19b（局部强调与封面色块）；陶棕 #896d59（细线与编号）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
暖象牙作主阅读底，深橄榄建立标题层次；灰玫瑰与鼠尾草保留为可见色块，陶棕点缀细线。封面可用暖象牙到灰玫瑰的渐变，正文保持浅底。原色不统一混白；Highlight 用暖象牙底配深橄榄字。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #394232。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #493c30。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #31392b。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #413f35。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #533835 / 字重 500；加粗字色 #394232 / 字重 700；Highlight 底色 #eee8da / 字重 400；Highlight 字色 #414c39 / 字重 400；链接字色 #37422f / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C15 蓝藻奶巧 · 配色

【任务】请按以下「C15 蓝藻奶巧配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C15 蓝藻奶巧」。装饰强调色 #3e3630，辅助色 #a79a8a；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fcf7df, #c7d3db)；更浅的辅助底 linear-gradient(135deg, #fcfaef, #f0f4f6)；装饰细线 linear-gradient(90deg, #c7d3db, #a79a8a)。奶油白与雾蓝承担大块浅底，灰棕作辅助色块，深巧克力用于标题和正文。用灰棕细线与雾蓝卡片区分层次；不把雾蓝或灰棕直接作白底小字。Highlight 采用奶油白配深巧克力。
原色色卡：雾蓝 #c7d3db（证据卡片与浅底）；灰棕 #a79a8a（辅助标签与色块）；奶油白 #fcf7df（阅读底与高亮底）；深巧克力 #3e3630（标题正文与加粗）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
奶油白与雾蓝承担大块浅底，灰棕作辅助色块，深巧克力用于标题和正文。用灰棕细线与雾蓝卡片区分层次；不把雾蓝或灰棕直接作白底小字。Highlight 采用奶油白配深巧克力。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #3e3630。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #4a5c69。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #3e3630。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #605951。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #6a5545 / 字重 500；加粗字色 #3e3630 / 字重 700；Highlight 底色 #fcf7df / 字重 400；Highlight 字色 #3e3630 / 字重 400；链接字色 #475d6d / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C16 蓝绿野餐 · 配色

【任务】请按以下「C16 蓝绿野餐配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C16 蓝绿野餐」。装饰强调色 #3b6ea5，辅助色 #94a02f；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #e3e0cb, #e3e0cb)；更浅的辅助底 linear-gradient(135deg, #f5f4ed, #eef1e3)；装饰细线 linear-gradient(90deg, #3b6ea5, #94a02f)。以象牙白为大面积底色，保留牛仔蓝与嫩苔绿的明确对比；靛蓝和松绿分别建立标题与辅助层级。蓝绿可用在并列模块和编号，但不自动代表好坏或涨跌。Highlight 用象牙白底配松绿字；渐变只作细线。
原色色卡：牛仔蓝 #3b6ea5（主装饰与编号）；象牙白 #e3e0cb（阅读底）；嫩苔绿 #94a02f（重点色块）；靛蓝 #0f304f（标题与加粗）；松绿 #394610（小标题与辅助文字）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
以象牙白为大面积底色，保留牛仔蓝与嫩苔绿的明确对比；靛蓝和松绿分别建立标题与辅助层级。蓝绿可用在并列模块和编号，但不自动代表好坏或涨跌。Highlight 用象牙白底配松绿字；渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #0f304f。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #394610。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #283740。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #5d6251。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #355e8c / 字重 500；加粗字色 #0f304f / 字重 700；Highlight 底色 #e3e0cb / 字重 400；Highlight 字色 #394610 / 字重 400；链接字色 #315b85 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C17 奶黄雾蓝 · 配色

【任务】请按以下「C17 奶黄雾蓝配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C17 奶黄雾蓝」。装饰强调色 #79a3ce，辅助色 #f0dc84；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f7f7ea, #f0dc84)；更浅的辅助底 linear-gradient(135deg, #f9faf0, #edf3f1)；装饰细线 linear-gradient(90deg, #79a3ce, #f0dc84)。暖白保持阅读空间，奶油黄作高亮和封面浅底，两层蓝分别作辅助底与主装饰。黄色与蓝色原值保留在色块，正文另用深灰蓝；不能把浅蓝或浅黄直接作白底小字。封面可由暖白渐变至奶油黄。
原色色卡：雾青蓝 #b4ccd2（辅助卡片底）；晴空蓝 #79a3ce（主装饰与编号）；奶油黄 #f0dc84（重点色块与高亮底）；暖白 #f7f7ea（长文阅读底）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
暖白保持阅读空间，奶油黄作高亮和封面浅底，两层蓝分别作辅助底与主装饰。黄色与蓝色原值保留在色块，正文另用深灰蓝；不能把浅蓝或浅黄直接作白底小字。封面可由暖白渐变至奶油黄。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #2f516e。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #49656e。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #33484d。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #5c6354。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #486474 / 字重 500；加粗字色 #2f516e / 字重 700；Highlight 底色 #f0dc84 / 字重 400；Highlight 字色 #3b4d51 / 字重 400；链接字色 #3f627f / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C18 青翡薄荷 · 配色

【任务】请按以下「C18 青翡薄荷配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C18 青翡薄荷」。装饰强调色 #008d74，辅助色 #47c4af；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #cfeae2, #b9e2d5)；更浅的辅助底 linear-gradient(135deg, #f2f8f5, #e6f4ee)；装饰细线 linear-gradient(90deg, #008d74, #47c4af)。按翡翠绿、清薄荷、浅玉青、柔薄荷、青白建立五层颜色。翡翠与清薄荷原值用于装饰块，浅玉青用于 Highlight，青白和柔薄荷用于阅读背景；小字另用深绿。不要把五种原色统一变成近白色，也不把同色阶误当作数值深浅。
原色色卡：翡翠绿 #008d74（主装饰与较大色块）；清薄荷 #47c4af（辅助色块）；浅玉青 #96d4c9（高亮底）；柔薄荷 #b9e2d5（卡片浅底）；青白 #cfeae2（主浅底）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
按翡翠绿、清薄荷、浅玉青、柔薄荷、青白建立五层颜色。翡翠与清薄荷原值用于装饰块，浅玉青用于 Highlight，青白和柔薄荷用于阅读背景；小字另用深绿。不要把五种原色统一变成近白色，也不把同色阶误当作数值深浅。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #12574b。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #34685d。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #243f39。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #4a655d。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #196b5a / 字重 500；加粗字色 #12574b / 字重 700；Highlight 底色 #96d4c9 / 字重 400；Highlight 字色 #17493f / 字重 400；链接字色 #1b6456 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C19 紫绿花园 · 配色

【任务】请按以下「C19 紫绿花园配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C19 紫绿花园」。装饰强调色 #667f60，辅助色 #cac4f6；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #edf2d4, #cac4f6)；更浅的辅助底 linear-gradient(135deg, #f7f8ee, #f1effb)；装饰细线 linear-gradient(90deg, #667f60, #cac4f6)。奶油白铺主阅读面，淡紫保留为可见高亮与封面区域，灰橄榄绿作装饰和细线，深砾灰压住文字层级。封面可用奶油白至淡紫的柔和渐变；长文仍用浅底。紫绿不是业务分类或正负编码，原色色块与可读字色分别使用。
原色色卡：奶油白 #edf2d4（阅读底）；淡长春紫 #cac4f6（封面色块与高亮底）；灰橄榄绿 #667f60（辅助装饰）；深砾灰 #4b4a3e（标题与正文）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
奶油白铺主阅读面，淡紫保留为可见高亮与封面区域，灰橄榄绿作装饰和细线，深砾灰压住文字层级。封面可用奶油白至淡紫的柔和渐变；长文仍用浅底。紫绿不是业务分类或正负编码，原色色块与可读字色分别使用。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #4b4a3e。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #4c5945。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #36392f。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #565649。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #5b4f71 / 字重 500；加粗字色 #4b4a3e / 字重 700；Highlight 底色 #cac4f6 / 字重 400；Highlight 字色 #433c50 / 字重 400；链接字色 #475a42 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C20 芭乐奶油 · 配色

【任务】请按以下「C20 芭乐奶油配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C20 芭乐奶油」。装饰强调色 #f19e96，辅助色 #b2ba69；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #f8ecd7, #fdd7d0)；更浅的辅助底 linear-gradient(135deg, #fcf8ee, #fff0ec)；装饰细线 linear-gradient(90deg, #f19e96, #b2ba69)。果浆奶白用于阅读区，芭乐浅粉用于封面浅底，果肉珊瑚和叶绿保留为明显装饰块；柚皮黄绿作 Highlight 底。整组原色偏浅，文字另配深橄榄、深棕和深珊瑚；不要拿珊瑚或叶绿原色承载白底小字。
原色色卡：芭乐浅粉 #fdd7d0（封面浅底）；柚皮黄绿 #eae7b4（高亮底）；果肉珊瑚 #f19e96（重点装饰色块）；芭乐叶绿 #b2ba69（辅助装饰）；果浆奶白 #f8ecd7（长文阅读底）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
果浆奶白用于阅读区，芭乐浅粉用于封面浅底，果肉珊瑚和叶绿保留为明显装饰块；柚皮黄绿作 Highlight 底。整组原色偏浅，文字另配深橄榄、深棕和深珊瑚；不要拿珊瑚或叶绿原色承载白底小字。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #4e522c。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #875548。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #3f392d。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #68634f。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #935146 / 字重 500；加粗字色 #4e522c / 字重 700；Highlight 底色 #eae7b4 / 字重 400；Highlight 字色 #4e522c / 字重 400；链接字色 #5b642e / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## C21 蓝棕咖啡 · 配色

【任务】请按以下「C21 蓝棕咖啡配色与文字」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
配色采用「C21 蓝棕咖啡」。装饰强调色 #79a3c3，辅助色 #957662；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #d2e2ec, #ebcdb7)；更浅的辅助底 linear-gradient(135deg, #f1f6f9, #faf0e8)；装饰细线 linear-gradient(90deg, #79a3c3, #957662)。灰牛仔蓝与浓咖啡构成冷暖对比，冰川蓝和杏米色承接阅读底，陶土棕用于细线与辅助块。深咖只用于文字或局部深色区域，不铺满整篇长文。Highlight 用杏米底配深咖字；图表分类色另按数据含义决定。
原色色卡：灰牛仔蓝 #79a3c3（主装饰色块）；浓咖啡 #3a2119（标题与加粗）；冰川蓝 #d2e2ec（证据卡片底）；杏米色 #ebcdb7（阅读底与高亮底）；陶土棕 #957662（辅助装饰）。保留这些原色在各自的装饰或背景用途，不统一混白；下方文字色为另配的可读色。
灰牛仔蓝与浓咖啡构成冷暖对比，冰川蓝和杏米色承接阅读底，陶土棕用于细线与辅助块。深咖只用于文字或局部深色区域，不铺满整篇长文。Highlight 用杏米底配深咖字；图表分类色另按数据含义决定。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #3a2119。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #485d6d。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #39322e。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #625952。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #784d36 / 字重 500；加粗字色 #3a2119 / 字重 700；Highlight 底色 #ebcdb7 / 字重 400；Highlight 字色 #3a2119 / 字重 400；链接字色 #425e74 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
配色只作用于页面排版和装饰。正文、标题与图例必须清晰可读；强调色不直接替代长段正文色。图表类型由问题与数据决定；保留已有图表的系列颜色含义、数据、单位、口径、来源、坐标轴、图例、visualMap 与交互，不批量覆盖现有图表配置。新增图表单独按数据选择配色，不把装饰渐变当作数值编码。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T01 横向条形图 · 图表

【任务】请按以下「T01 横向条形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T01 横向条形图 / Horizontal bar」。适合的问题：比较类别大小，尤其是名称较长时。
需要字段：类别、同口径数值。按值排序，保留有业务含义的原顺序；标明单位。
使用边界：数量从零起画；类别重叠时不能解读为总体构成。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T02 纵向柱形图 · 图表

【任务】请按以下「T02 纵向柱形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T02 纵向柱形图 / Column chart」。适合的问题：比较少量类别或离散期间。
需要字段：类别或期间、数值。每个柱子对应一个明确对象；保持零基线和一致尺度。
使用边界：不截断数量轴夸大差异；类别多或标签长时改横向。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T03 分组柱形图 · 图表

【任务】请按以下「T03 分组柱形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T03 分组柱形图 / Grouped bar」。适合的问题：同一维度内比较不同系列。
需要字段：类别、系列、同口径数值。系列并列且颜色映射稳定，保留各系列直接标签或图例。
使用边界：各组时期、范围与单位一致；系列过多改分面。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T04 发散条形图 · 图表

【任务】请按以下「T04 发散条形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T04 发散条形图 / Diverging bar」。适合的问题：比较相对零点的正负差值。
需要字段：类别、有符号差值、单位。显示零线、正负号和实际单位，明确相对什么基准变化。
使用边界：差值与同比不可混称；没有参照点不能人为设定正负。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T05 棒棒糖图 · 图表

【任务】请按以下「T05 棒棒糖图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T05 棒棒糖图 / Lollipop chart」。适合的问题：用轻量图形比较少量类别。
需要字段：类别、同口径数值。圆点位置编码数值，细线连到共同零基线，保证标签可读。
使用边界：圆点大小不承载额外含义；类别太多应改普通条形。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T06 哑铃图 · 图表

【任务】请按以下「T06 哑铃图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T06 哑铃图 / Dumbbell chart」。适合的问题：比较同一对象的两个可比状态。
需要字段：对象、状态甲数值、状态乙数值。每行一个对象，用两端点与连接线展示前后或两组差异。
使用边界：两个点须来自同一对象与一致口径，连线本身不证明因果。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T07 坡度图 · 图表

【任务】请按以下「T07 坡度图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T07 坡度图 / Slope chart」。适合的问题：看两个离散时点之间谁升谁降。
需要字段：对象、两个可比时点、数值。仅连接已知端点并显示两端值，保持共同尺度，避免把连线称为连续趋势。
使用边界：仅两点不能说明中间轨迹；线的倾斜角受尺度影响。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T08 子弹图 · 图表

【任务】请按以下「T08 子弹图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T08 子弹图 / Bullet chart」。适合的问题：看实际值相对目标的位置。
需要字段：对象、实际值、目标值、范围。用短条表示实际值、垂直标记表示目标，背景范围仅使用已定义界限。
使用边界：目标与背景区间须有明确来源；不能凭视觉填出好坏档位。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T09 折线图 · 图表

【任务】请按以下「T09 折线图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T09 折线图 / Line chart」。适合的问题：看有序时间上的变化。
需要字段：有序时间、数值、可选系列。时间按真实间距排列，标出首末期间、单位与缺失值处理。
使用边界：不连接不同口径，不把缺失期当零，不随意平滑制造峰谷。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T10 阶梯折线图 · 图表

【任务】请按以下「T10 阶梯折线图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T10 阶梯折线图 / Step line」。适合的问题：看在某时刻生效并持续的状态。
需要字段：变更时点、状态值、生效规则。按实际生效时点绘制水平保持段与跳变，并说明采用左连续或右连续。
使用边界：明确变更在区间起点还是终点生效，不把连续变量硬画成台阶。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T11 面积图 · 图表

【任务】请按以下「T11 面积图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T11 面积图 / Area chart」。适合的问题：强调单一序列随时间的量级。
需要字段：有序时间、非负可加数值。在单一折线下填浅色，保留零基线与清晰边界，不用渐变暗示额外变量。
使用边界：面积从零起画；重叠面积不适合精确比较多个系列。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T12 堆叠面积图 · 图表

【任务】请按以下「T12 堆叠面积图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T12 堆叠面积图 / Stacked area」。适合的问题：看总体及其组成随时间的变化。
需要字段：时间、互斥成分、非负可加数值。固定系列顺序和颜色，展示成分值及总量；比较各自走势时改多条折线。
使用边界：成分须同分母且能相加；中间层不能直接按顶部高度读值。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T13 小多图 · 图表

【任务】请按以下「T13 小多图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T13 小多图 / Small multiples」。适合的问题：在统一坐标下比较多个系列。
需要字段：时间、系列、同单位数值。各分面共用单位、时间窗、轴范围和类别顺序，不压缩字号。
使用边界：比较量级时所有小图同尺度；若使用独立尺度需醒目标明。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T14 排名变化图 · 图表

【任务】请按以下「T14 排名变化图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T14 排名变化图 / Bump chart」。适合的问题：比较对象的名次随时间变化。
需要字段：时间、对象、排名、并列规则。纵轴用数值名次并将第一名置顶，端点标明对象，不将名次落差解释为规模差。
使用边界：排名不代表数值差距；补齐并列、退出和新增对象的处理规则。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T15 饼图 · 图表

【任务】请按以下「T15 饼图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T15 饼图 / Pie chart」。适合的问题：粗看少量互斥部分占整体的比例。
需要字段：类别、非负可加数值、统一总量。说明分母与总体，展示类别和比例，不使用三维或爆炸效果。
使用边界：类别必须互斥且穷尽或明确剩余；差异很小时改条形。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T16 环形图 · 图表

【任务】请按以下「T16 环形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T16 环形图 / Donut chart」。适合的问题：展示少量构成并补充整体说明。
需要字段：类别、数值、统一总量。环片按真实占比绘制，中心只放有依据的总量或说明，保留类别与比例。
使用边界：中间文字不能替代分母；类别多或需精确比较时改条形。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T17 堆叠柱形图 · 图表

【任务】请按以下「T17 堆叠柱形图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T17 堆叠柱形图 / Stacked bar」。适合的问题：比较多组总量和内部组成。
需要字段：组别、互斥成分、非负可加数值。组内堆叠、组间保留共同零基线，工具提示列出各成分及总量。
使用边界：成分和总量必须可加；不同分母的百分比不能直接堆叠。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T18 百分比堆叠条 · 图表

【任务】请按以下「T18 百分比堆叠条图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T18 百分比堆叠条 / 100% stacked bar」。适合的问题：比较不同组的构成比例。
需要字段：组别、成分分子、组内分母。按组内真实分母归一到 100%，显示比例与分母，明确四舍五入误差。
使用边界：总量不同的组归一化后不能比较规模；必须保留各组样本量。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T19 瀑布图 · 图表

【任务】请按以下「T19 瀑布图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T19 瀑布图 / Waterfall」。适合的问题：解释起点到终点的可加变化。
需要字段：起点、有符号变动项、终点。用起终点柱与增减浮动柱构成桥接，显示符号并校验起点加变动等于终点。
使用边界：所有变动项必须与终点对账，不能把相关因素当作可加贡献。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T20 帕累托图 · 图表

【任务】请按以下「T20 帕累托图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T20 帕累托图 / Pareto chart」。适合的问题：看少数类别贡献了多少累计总量。
需要字段：互斥类别、非负可加数值。按数值降序排列柱形，累计曲线从同一数组计算，区分原值轴与百分比轴。
使用边界：累计百分比须由同一排序与分母计算；不强行套用二八法则。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T21 直方图 · 图表

【任务】请按以下「T21 直方图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T21 直方图 / Histogram」。适合的问题：看连续变量的分布形状。
需要字段：逐条数值、分箱边界、样本量。使用明确且不重叠的数值分箱，相邻箱紧接；标出频数或密度及样本量。
使用边界：箱宽会影响外观；不等宽箱用密度，边界开闭与缺失处理须明确。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T22 箱线图 · 图表

【任务】请按以下「T22 箱线图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T22 箱线图 / Box plot」。适合的问题：比较不同组的中位数与离散程度。
需要字段：组别、原始观测或经核验的分位统计。从真实观测计算分位数，写明须线与异常点定义，不仅展示均值。
使用边界：必须说明四分位数算法和须线规则；须端不一定是最大最小值。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T23 点带分布图 · 图表

【任务】请按以下「T23 点带分布图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T23 点带分布图 / Strip plot」。适合的问题：在样本较少时保留每个观测点。
需要字段：稳定观测 ID、组别、数值。每个点对应一条记录，沿数值轴定位；分组方向的微小错位不承载数值。
使用边界：抖动只用于防重叠，不代表第二个变量；过多样本要控制遮挡。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T24 点与区间图 · 图表

【任务】请按以下「T24 点与区间图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T24 点与区间图 / Point and interval」。适合的问题：同时展示估计与已知上下界。
需要字段：对象、估计值、下界、上界、区间含义。用点表示估计、线段表示真实区间，标明区间类型、方法及必要样本信息。
使用边界：无估计方法和置信水平不能称置信区间；区间重叠不等于差异检验。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T25 散点图 · 图表

【任务】请按以下「T25 散点图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T25 散点图 / Scatter plot」。适合的问题：看两个数值变量的关系。
需要字段：观测 ID、数值 X、数值 Y、同一粒度。明确两轴含义、单位与观测粒度，每个点可回查来源，保留缺失与异常处理。
使用边界：相关不代表因果；不要混合总体和明细，注意分母共用造成的关系。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T26 气泡图 · 图表

【任务】请按以下「T26 气泡图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T26 气泡图 / Bubble chart」。适合的问题：在散点上加入第三个量级。
需要字段：观测 ID、X、Y、非负大小值。位置表示 X/Y，气泡面积与第三变量成比例，并说明大小含义与图例。
使用边界：用面积而非半径正比编码大小；重叠与极大气泡会掩盖对象。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T27 四象限图 · 图表

【任务】请按以下「T27 四象限图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T27 四象限图 / Quadrant scatter」。适合的问题：按两个有业务含义的阈值分区。
需要字段：对象、X、Y、有来源的横纵阈值。使用真实量化轴和已说明依据的阈值线，保留点位数值与边界对象。
使用边界：阈值不能为了好看设在中点；象限名称不自动证明战略价值。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T28 矩阵热力图 · 图表

【任务】请按以下「T28 矩阵热力图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T28 矩阵热力图 / Heatmap」。适合的问题：找两维组合中的高低与稀疏。
需要字段：行维度、列维度、数值、覆盖规则。每格对应一个有明确口径的组合，固定色阶并提供图例、数值与缺失说明。
使用边界：零与缺失分开；顺序量用连续色阶，正负量才考虑分歧色阶。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T29 雷达图 · 图表

【任务】请按以下「T29 雷达图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T29 雷达图 / Radar chart」。适合的问题：概览少量对象的可比多维轮廓。
需要字段：对象、维度、同量纲或冻结标准化后的值。所有维度明确量表、方向和范围，保留原始数值及标准化方法，限制对象数量。
使用边界：不能凭印象打分；维度顺序影响形状，面积不宜用来排名。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T30 漏斗图 · 图表

【任务】请按以下「T30 漏斗图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T30 漏斗图 / Funnel」。适合的问题：展示同一批对象在有序阶段中的保留。
需要字段：同一队列、有序阶段、去重人数或一致计数。核验队列、事件、时间窗与分母后再绘制阶段量；证据不足时改独立指标而非漏斗。
使用边界：搜索、阅读、购买等独立证据链不能拼接；重复进入须有处理口径。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T31 桑基图 · 图表

【任务】请按以下「T31 桑基图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T31 桑基图 / Sankey diagram」。适合的问题：展示可核验的量在节点间如何分配。
需要字段：来源节点、目标节点、非负流量、连接依据。线宽编码真实流量，节点与连接可回查，并解释损耗、遗漏或流量不守恒原因。
使用边界：线宽必须有流量依据；共现不等于迁移，内部节点需要核对收支。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T32 关系网络图 · 图表

【任务】请按以下「T32 关系网络图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T32 关系网络图 / Network graph」。适合的问题：查看有证据支持的对象连接。
需要字段：节点 ID、边的两端、关系类型、可选权重。明确每条边的业务含义及有向性，保留节点与边的来源，布局只服务阅读。
使用边界：距离与中心位置不自动代表重要性；箭头和因果含义须有依据。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T33 树状层级图 · 图表

【任务】请按以下「T33 树状层级图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T33 树状层级图 / Tree diagram」。适合的问题：展示明确的父子归属。
需要字段：唯一节点 ID、父节点 ID、名称。按已定义分类树展开父子关系，保证节点可定位，长标签与分支可读。
使用边界：一个节点多父级时不再是简单树；层级不等于因果。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T34 矩形树图 · 图表

【任务】请按以下「T34 矩形树图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T34 矩形树图 / Treemap」。适合的问题：同时查看层级归属与可加规模。
需要字段：父子层级、叶子非负数值、汇总规则。矩形面积编码叶子规模，颜色用于有限类别区分，提供层级路径、数值与总量。
使用边界：面积难做精确比较；父值要与子项对账，不能混用单位。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T35 旭日图 · 图表

【任务】请按以下「T35 旭日图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T35 旭日图 / Sunburst」。适合的问题：从中心向外阅读分层构成。
需要字段：父子层级、叶子数值、统一总量。使用真实层级与可加值，解释扇区角度的构成含义，保留路径、数值和返回入口。
使用边界：外圈面积受半径影响，不能把外圈更大理解为贡献更高。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T36 日历热力图 · 图表

【任务】请按以下「T36 日历热力图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T36 日历热力图 / Calendar heatmap」。适合的问题：观察逐日节奏与周期。
需要字段：真实日期、每日指标、覆盖状态。以真实日历定位日期，固定指标与色阶，分别标注零值和缺失，并保留日期查询。
使用边界：没有记录不一定为零；不跨口径或时区直接比较，不能制造完整日历。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T37 甘特图 · 图表

【任务】请按以下「T37 甘特图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T37 甘特图 / Gantt chart」。适合的问题：看事项起止与时间上的重叠。
需要字段：事项、开始时间、结束时间、可选状态。条形长度对应真实持续时间，明确计划或实际、起止日期及边界约定。
使用边界：计划与实际区分，标明结束边界；时间条不能自动推导依赖或完成度。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T38 仪表图 · 图表

【任务】请按以下「T38 仪表图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T38 仪表图 / Gauge」。适合的问题：查看单一指标在固定范围中的位置。
需要字段：指标值、下限、上限、目标或阈值依据。说明量程与指标含义，只显示有依据的目标和阈值；需精确比较多个对象时用条形。
使用边界：范围不能随值随意改变；装饰色带不等于实际风险或好坏。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T39 平行坐标图 · 图表

【任务】请按以下「T39 平行坐标图图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T39 平行坐标图 / Parallel coordinates」。适合的问题：查看多个观测在多维数值上的组合。
需要字段：观测 ID、多维数值、各维量纲与方向。每条线对应一个对象，逐轴标出含义与范围，保持标准化方法和高低方向可解释。
使用边界：轴顺序会改变视觉关系；标准化、反向轴及筛选规则须写清。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## T40 词云 · 图表

【任务】请按以下「T40 词云图表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
请根据本次问题与真实数据，判断是否适合「T40 词云 / Word cloud」。适合的问题：快速浏览词语与相对出现频次。
需要字段：词语、真实频数、统计单位、清洗与去重规则。依据真实文本统计词频并保留分词、同义词合并、停用词和计数单位；字号只辅助浏览，附精确频数表。
使用边界：不能用词云判断需求份额、情感或因果；精确比较频次请用条形或表格。字段或条件不足时说明缺口，允许选择其他更合适的图型；收藏图型不构成默认图型或白名单。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
页面渐变只用于背景和装饰，不当作数值编码。图表按顺序、类别或正负含义单独配色；保留可读标签、图例、单位、时间、来源、分母和缺失说明，手机端可阅读。已有图表的数据、系列颜色含义、坐标轴、visualMap 和交互不批量改写。
收藏册图型示意数据不可复制进业务报告。只使用提供或核验后的真实数据；数据不足保留注明缺口的空位，不补造指标、分数、阈值、关系或结论。搜索、阅读、评论与购买不能无连接依据拼成漏斗或流向。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B01 简洁三线表 · 表格

【任务】请按以下「B01 简洁三线表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：报告正文中的清爽数据表。
需要材料：字段名、真实行数据、单位和来源。
使用语义化 table、caption、thead、tbody 和 scope；只保留表顶、表头底、表底三条线（2px / 1px / 1px），无竖线。文本左对齐、数值右对齐并用等宽数字；表头 14px/600，正文 16px，单元格内边距 12px 16px。单位、时间、分母、来源和缺失说明就近保留。
窄屏在带名称、可键盘聚焦的表格容器内横向滚动；不让整页横向溢出。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B02 浅色斑马纹 · 表格

【任务】请按以下「B02 浅色斑马纹」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：长表逐行查阅，降低串行风险。
需要材料：同一粒度的行数据、列定义、单位及来源。
保留完整表格语义；隔行使用当前色板的最浅底色，另一行白底；表头半粗，行间用 1px 细线。每行高度由内容决定，数值右对齐。斑马纹只帮助定位，不表达好坏或数值强弱。
窄屏保留列标题和行对应关系，在独立容器内滚动；来源在容器下方可直接阅读。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B03 品牌与方案对比表 · 表格

【任务】请按以下「B03 品牌与方案对比表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：同一把尺子比较多个对象。
需要材料：对象清单、统一比较维度、每个单元格的材料与来源。
第一列放评价维度，后续列放品牌或方案；首列使用 scope=row，表头使用 scope=col。首列可 sticky left:0，并设置不透明底色，行内横向滚动时仍知道在比什么。只有已提供且同口径的信息才能比较；未知明确标注，不凭空打分或标最佳。
窄屏表格横向滚动、首列保持可见；首列宽度不超过可视区域的 40%，长文可展开。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B04 重点行列高亮 · 表格

【任务】请按以下「B04 重点行列高亮」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：定位用户指定或有依据的重点。
需要材料：真实表格、需要强调的行列及强调依据。
整张表保持中性，仅给用户指定或有证据的行/列添加浅底，并用文字标签“关注项”解释。不能单靠颜色表达含义，不默认第一行最佳。正文用可读字色，重点数值可 700 字重，其他数值完整保留。
窄屏保留关注标签和对应字段；重点不依赖悬停可见。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B05 指标与口径说明表 · 表格

【任务】请按以下「B05 指标与口径说明表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把数字和解释放在一起，方便核验。
需要材料：已核验指标、口径、时期、单位、分母及来源。
列出指标名、数值、单位、时间、口径或分母、来源。描述很长时用详情行展开，决定结论的边界仍直接显示。空值区分“缺失”“不适用”和真实零；不把同比、差值、百分点混为一谈。
窄屏可变成每条指标的 dl 字段卡，保留字段名与值；不能仅显示一个大数字。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E01 一句话结论卡 · 结论与证据

【任务】请按以下「E01 一句话结论卡」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：一条判断，连接支撑与行动。
需要材料：已有判断、支撑材料、边界和下一步。
使用轻浅底和左侧 3px 强调线。依次显示用户已有的一句判断、最必要的支撑、适用边界和行动含义；结论 20–24px/700，证据正文 16px。没有足够证据时显示“待验证判断”，不把排版突出当成事实认证。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E02 原声与来源卡 · 结论与证据

【任务】请按以下「E02 原声与来源卡」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：保留完整语境的消费者或访谈引用。
需要材料：可核验原文、来源及必要上下文。
使用 blockquote 引用用户提供的原文，cite 标出真实来源，另列背景和分析。原文与分析在视觉上分开；删节必须用省略标记并保留回查入口，不伪造说话者、引语或引用日期。引用 18px/1.8，来源 14px。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E03 补充说明卡 · 结论与证据

【任务】请按以下「E03 补充说明卡」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：放方法、术语和阅读提醒。
需要材料：需要解释的术语、方法或补充材料。
使用 1px 细边框和低饱和浅底，标题 16px/600、正文 16px/1.75。关键限制常驻显示；仅附加解释可用 details/summary，支持键盘、打印展开。不用警告色装饰普通说明。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E04 证据状态标签 · 结论与证据

【任务】请按以下「E04 证据状态标签」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：区分事实、推断与待验证。
需要材料：每条主张的已知证据状态与判定依据。
用文字标签明确区分“已核验事实”“来源方主张”“合理推断”“待验证”；状态依据来自材料或用户确认。颜色仅辅助识别，不自行升级状态。标签旁提供支持依据或最小补证；不得通过颜色暗示可信评分。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## Y01 舒展阅读 · 文字层级

【任务】请按以下「Y01 舒展阅读」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：适合长文、洞察叙事。
需要材料：正文层级、内容与所选字体。
正文 18px / 1.85，段间距 28px；页面主标题 48px/1.2，章节标题 32px/1.35，小标题 23px/1.45；标题字重 700，图注和来源 14px/1.6。正文容器最大宽 760px，长段单栏宽不超过约 38 个汉字；全幅图表可突破正文行宽。手机主标题最多 32px，其他字号不因内容多而缩小；间距可以收紧但不能删内容。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 48px，字重 700，行高 1.2，段后距 20px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 32px，字重 700，行高 1.35，段后距 18px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 18px，字重 400，行高 1.85，段后距 28px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.6，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 23px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## Y02 标准报告 · 文字层级

【任务】请按以下「Y02 标准报告」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：适合图文与数据交替。
需要材料：正文层级、内容与所选字体。
正文 16px / 1.75，段间距 22px；页面主标题 40px/1.2，章节标题 28px/1.35，小标题 21px/1.45；标题字重 700，图注和来源 14px/1.6。正文容器最大宽 1100px，长段单栏宽不超过约 38 个汉字；全幅图表可突破正文行宽。手机主标题最多 32px，其他字号不因内容多而缩小；间距可以收紧但不能删内容。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.2，段后距 20px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 28px，字重 700，行高 1.35，段后距 18px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 22px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.6，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 21px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## Y03 紧凑查阅 · 文字层级

【任务】请按以下「Y03 紧凑查阅」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：适合资料手册、对照信息。
需要材料：正文层级、内容与所选字体。
正文 16px / 1.6，段间距 16px；页面主标题 34px/1.2，章节标题 25px/1.35，小标题 20px/1.45；标题字重 700，图注和来源 14px/1.6。正文容器最大宽 1200px，长段单栏宽不超过约 38 个汉字；全幅图表可突破正文行宽。手机主标题最多 32px，其他字号不因内容多而缩小；间距可以收紧但不能删内容。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 34px，字重 700，行高 1.2，段后距 20px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 25px，字重 700，行高 1.35，段后距 18px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.6，段后距 16px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.6，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## S01 细边框留白 · 卡片装饰

【任务】请按以下「S01 细边框留白」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：克制、清楚，适合密集信息。
需要材料：需要分组的真实内容。
白色卡片，1px #e8ebf0 边框、12px 圆角、24px 内边距，无阴影；卡片间距 20px。边框划分内容组，不表示量级。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## S02 柔和悬浮 · 卡片装饰

【任务】请按以下「S02 柔和悬浮」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：轻层次，适合重点模块。
需要材料：主次关系已明确的内容。
白色卡片，16px 圆角、28px 内边距，阴影 0 8px 28px rgba(39,52,69,.07)，配 1px #e8ebf0 细边框。仅为层级服务，不让所有模块竞争焦点。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## S03 渐变卡头 · 卡片装饰

【任务】请按以下「S03 渐变卡头」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：延续粉蓝渐变的轻盈感。
需要材料：标题、正文和所选浅底渐变。
卡片正文白底，16px 圆角，只有卡头使用当前浅底渐变；标题使用实际可读标题色。正文 24px 内边距，卡头与正文用 1px 细线分开；不把渐变扩散到数据编码。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## S04 侧边重点线 · 卡片装饰

【任务】请按以下「S04 侧边重点线」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：给关键说明一个清楚落点。
需要材料：需要提示的内容及强调依据。
白底、12px 圆角，左侧 3px 当前装饰强调色，其他边 1px #e8ebf0；24px 内边距。重点仍需标题或文字标识，不靠色线改变结论等级。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## S05 细线章节分隔 · 卡片装饰

【任务】请按以下「S05 细线章节分隔」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：用留白和细线代替大卡片。
需要材料：章节或内容组及顺序。
内容不包围成卡片；章节间使用 1px #e8ebf0 水平线和 32px 上下间距；可在章标题下加 48px 长、2px 高的当前强调色细线。保留真实标题层级与连贯阅读。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## A01 重点数据标签 · 图表标注

【任务】请按以下「A01 重点数据标签」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：让关键值无需悬停就能读到。
需要材料：真实序列、单位、需要标记的对象或规则。
仅对用户指定或满足已确认规则的关键数据加直接标签，显示对象、数值和单位；数值必须从原序列读取。避免全部标签相互覆盖；重要信息也保留在数据表里。改变标注，不改数值、尺度和排序。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## A02 峰值与异常注释 · 图表标注

【任务】请按以下「A02 峰值与异常注释」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：解释一个值得关注的位置。
需要材料：完整序列、时间、峰值或异常规则、注释来源。
从完整真实序列核实峰值；并列峰值不能只挑有利项。用细引线和短注释标记；异常需要明确检测规则，峰值不自动叫异常。解释与原因要有证据，不用走势推断因果；不平滑数据制造峰谷。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## A03 有依据的参考线 · 图表标注

【任务】请按以下「A03 有依据的参考线」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：展示目标、均值或阈值的位置。
需要材料：真实数据、参考值、口径和来源。
仅在用户提供或能依据已确认口径计算的参考值上画虚线；标签写明值、单位、含义及来源。均值注明加权或非加权、范围；阈值不得为了好看取中点。无来源时保留普通图，不画参考线。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## A04 图例与单位栏 · 图表标注

【任务】请按以下「A04 图例与单位栏」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：先说明颜色和数字代表什么。
需要材料：系列定义、单位、范围、分母和颜色含义。
图例保持真实系列顺序和含义，用颜色加文字、必要时加形状共同辨识；单位靠近标题或轴，注明时期与分母。渐变仅在连续数值色阶确有含义时使用，不把页面渐变复制到分类编码。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## A05 图下注释与来源 · 图表标注

【任务】请按以下「A05 图下注释与来源」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把解释、限制和来源就近放好。
需要材料：已支持的观察、限制、来源、时间与统计口径。
图下依次给出用户已有的观察、必要限制及可核验来源；来源 14px/1.6，文本自然换行。长网址使用明确标签；重要边界不可只藏在 tooltip。图表可交互，静态阅读也能理解口径。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## P01 洞察报告组合 · 整页组合

【任务】请按以下「P01 洞察报告组合」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：分屏开篇 → 证据与侧注 → 结论行动。
需要材料：正文、真实图表数据、可选主视觉及来源。
开篇采用文字与主视觉分屏；正文让主要图表占较大空间、解释侧注占较小空间；随后放可核验明细表和结论行动。没有主视觉时使用浅底标题区，不编造图片或观点。模块数量按内容增减。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
组合中的布局定义：
分屏封面：开篇用文字与主视觉分屏：一侧呈现主题、一句判断与必要背景，另一侧放与主题直接相关的真实图片或有明确含义的视觉。 窄屏按标题、说明、主视觉堆叠，确保首屏能读懂主题。
主图侧注：用较宽的主区域呈现本次合适的图表、图片或表格，旁边安排观察、解释与行动含义，来源与口径紧邻证据。 窄屏先呈现主证据，再按顺序显示侧注；宽表在自身区域滚动。
决策摘要页：以决策摘要页组织内容：开头点明本次要决定的问题和有依据的判断，接续必要证据与适用边界，末尾给出明确行动或待决事项，并提供完整材料入口；模块数量由内容决定。 窄屏按判断、证据、限制、行动单列展开，长内容自然增高，不压缩成固定一屏。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## P02 对比提案组合 · 整页组合

【任务】请按以下「P02 对比提案组合」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：决策开篇 → 对比材料 → 选择与代价。
需要材料：待决问题、可比较对象、统一维度及证据。
顶部用决策摘要交代要决定什么，中部并列方案与统一维度的完整对比表；图表使用与问题相适配的类型，末尾显示条件、代价与行动。评价标准不足时不自动选赢家。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
组合中的布局定义：
决策摘要页：以决策摘要页组织内容：开头点明本次要决定的问题和有依据的判断，接续必要证据与适用边界，末尾给出明确行动或待决事项，并提供完整材料入口；模块数量由内容决定。 窄屏按判断、证据、限制、行动单列展开，长内容自然增高，不压缩成固定一屏。
多方案对比：将多个备选方案按统一维度并列比较，完整展示适用条件、支撑依据、投入与限制，长字段允许扩展；只有评价规则和证据充分时才标注推荐，不用装饰面积或虚构分数制造高低。 窄屏转为带维度标签的方案分组，或在独立对照表内横向滚动；保留对象和维度对应关系。
矩阵看板：按两个有明确含义的维度构成内容矩阵，每格放对应观点与材料；保留行列标题及空白原因，不编造强弱或量化着色。 窄屏保留矩阵语义，可在独立区域横向滚动或转成带行列标签的分组清单。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## P03 案例复盘组合 · 整页组合

【任务】请按以下「P03 案例复盘组合」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：主题开篇 → 过程与证据 → 复盘行动。
需要材料：案例背景、真实过程、原声或材料、结果与来源。
简洁主题开篇，正文以纵向叙事展开事件与已知证据，使用原声引用保留上下文，末尾呈现可核验结果和下一步。事件时间与因果分别说明；章节和长度由材料决定。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
组合中的布局定义：
全幅开场：用全幅主视觉构成开场，将短标题与一句引言安排在清晰区域；长正文、图表和方法说明放到后续独立阅读区域。 窄屏调整图片裁切与文字位置，保留主体和可读对比。
纵向叙事：沿纵向组织问题、证据、解释和行动等实际段落，适度错开位置制造节奏；段落数由内容决定，保留每一步的依据与边界。 窄屏使用顺畅单列，减少左右错位和多余留白。
原声拼贴：将已核验的原声按主题组织成大小有别的引文区，突出代表性表达，保留完整语境、来源与不同看法，再给出有边界的归纳。 窄屏单列呈现原声，不截断长引文。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N01 保留原图的案例封面 · 笔记封面

【任务】请按以下「N01 保留原图的案例封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：完整呈现笔记原图，适合案例与素材证据。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
白色卡片、14px 圆角、1px 细边框；图片区默认 3:4 作为设计容器，object-fit:contain 保留原图与文字，剩余空间用浅底填充。图片下依次放完整标题 18px/700、来源与日期 14px；内边距 18px。整张链接可包含图片、标题和来源，不隐藏回查信息。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
手机端单列，封面最大宽度 360px 并居中，标题自然换行；点击后能看清完整原图。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N02 杂志式图文封面 · 笔记封面

【任务】请按以下「N02 杂志式图文封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：标题、视觉和留白组成一张轻盈封面。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
适用于新设计封面：白底、20px 内边距；顶部小栏目标签 12px，主标题 28px/1.3，接近方形的主图区居中，底部放一句提供的摘要与来源。主图与标题不相互覆盖，装饰短线使用当前强调色。已有原图用 contain；只有确认可裁切的装饰照片可 cover。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
手机端保持标题—图片—来源顺序，标题可到 24px，内容增高；不截断长标题。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N03 渐变叠字封面 · 笔记封面

【任务】请按以下「N03 渐变叠字封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把新封面的主视觉与大标题放在一起。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
默认 3:4 设计容器，主视觉铺在底层，下方叠浅底渐变或实色文字面板；标题 26–32px/700，留出四周 20px 安全边距。选择当前实际可读字色，按最终图片和渐变最不利位置检查对比度，不足时改为不透明浅底。仅用于允许重新设计的素材；原笔记截图不覆盖文字与证据。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
窄屏标题自然换行并允许卡片变高；不固定行数，不靠缩字隐藏信息。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N04 左图右文入口 · 笔记封面

【任务】请按以下「N04 左图右文入口」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：适合正文中的笔记引用与延伸阅读。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
横向卡片，左侧缩略图宽 104px、比例 3:4、contain，右侧为 18px 半粗标题、摘要、14px 来源和明确“查看详情”文案。间距 16px，卡片内边距 16px、12px 圆角。图片和文字是同一详情入口，其他操作放在链接外。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
容器小于 360px 时图宽降到 80px，长标题自然换行；仍放不下则上下排列，不产生页面横向滚动。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N05 系列编号文字封面 · 笔记封面

【任务】请按以下「N05 系列编号文字封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：统一一组专题、清单或栏目封面。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
适用于新设计封面：使用当前浅底渐变和少量细线；顶部系列名 14px，中部大编号 48–64px 与完整主题 28px/1.3，底部放提供的栏目说明。编号仅为展示顺序，不能暗示排名或效果；系列的字体、标题位置和边距 24px 保持一致。默认 3:4，实际比例可随素材和任务调整。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
手机端保持阅读顺序，编号可缩到 40px，标题不小于 24px，长文可以增加高度。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N06 双列笔记合集 · 笔记封面

【任务】请按以下「N06 双列笔记合集」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把多篇笔记组织成可回查的阅读入口。
需要材料：真实封面或截图、完整标题、作者或来源、可选日期、真实详情或原文链接；没有的字段不补造。
外层用 CSS Grid 两列，间距 16–20px；每张包含完整封面、18px 标题、14px 来源和独立详情链接。封面容器可统一 1:1，原图 object-fit:contain 保留全部信息；只对确认可裁的装饰照片用 cover。按提供的顺序从左到右、从上到下排列，不以瀑布流重排阅读顺序；数量由真实材料决定。 封面示例仅用于设计，正式页面使用提供的素材；不捏造作者、点赞、收藏、销量或效果。已有笔记原图内的标题与证据不可重写、抹除或裁掉。标题、来源、日期在图片外保留可读文字，图片有恰当 alt；装饰图用空 alt。导航用真实 a 元素，默认链接到已存在的详情；外部原文需用户提供真实 URL，新标签页须提示并加 rel="noopener noreferrer"。缺链接时显示“原文链接待提供”，不做空链接或伪造平台 URL。收藏按钮与链接平级，避免嵌套点击区域。 封面比例是可选设计参数，不代表平台官方规格。
小于 600px 时改为单列；每篇维持独立标题与链接，触控入口清晰。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J01 页内锚点跳转 · 点击与跳转

【任务】请按以下「J01 页内锚点跳转」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：点击封面后，定位到本页完整案例。
需要材料：入口标题与封面、目标详情内容或真实原文 URL、所需打开方式；来源信息仅使用已提供材料。
入口用 a href="#唯一详情ID"；详情保留可聚焦标题或 tabindex="-1" 容器，跳转时更新 URL 片段、滚动到可见区域并把焦点移到详情。固定导航下给目标设置 scroll-margin-top。详情若折叠，先展开；支持首次带片段打开、刷新、前进与后退，并有返回目录入口。尊重减少动态效果设置。 不伪造笔记地址、作者和互动数据；没有目标内容或链接时明确显示缺口。页面内用 a 跳转，展开与弹窗用 button 或 details/summary；不能只给 div 加点击，也不能在整卡链接中嵌套按钮。键盘焦点清楚，触控目标建议至少 44px；图标旁有说明文字。打开方式与文字提示一致，内容和图表不因导航样式而删改。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J02 新标签页看原文 · 点击与跳转

【任务】请按以下「J02 新标签页看原文」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：保留报告阅读位置，同时回查原笔记。
需要材料：入口标题与封面、目标详情内容或真实原文 URL、所需打开方式；来源信息仅使用已提供材料。
使用用户提供的有效 HTTPS 原文地址作为 a href，target="_blank"，rel="noopener noreferrer"；链接文案明确“查看原文（新标签页）”。不使用脚本拼接伪链接或定时弹窗；缺地址时显示“原文链接待提供”，不要给无目标元素添加可点击样式。链接打开后保留本页阅读位置。收藏册演示在新标签页打开站内示意原文，正式应用须替换为真实链接。 不伪造笔记地址、作者和互动数据；没有目标内容或链接时明确显示缺口。页面内用 a 跳转，展开与弹窗用 button 或 details/summary；不能只给 div 加点击，也不能在整卡链接中嵌套按钮。键盘焦点清楚，触控目标建议至少 44px；图标旁有说明文字。打开方式与文字提示一致，内容和图表不因导航样式而删改。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J03 弹窗预览详情 · 点击与跳转

【任务】请按以下「J03 弹窗预览详情」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：先快速读封面与摘要，再决定是否打开原文。
需要材料：入口标题与封面、目标详情内容或真实原文 URL、所需打开方式；来源信息仅使用已提供材料。
入口用 button 打开原生 dialog.showModal()，由 aria-labelledby 指向清楚标题。弹窗最大宽 720px、最大高 85dvh、内容区可滚动，顶部关闭按钮持续可见。打开时移入焦点；支持 Escape、关闭按钮和 Tab 焦点限制，关闭后焦点回到原触发按钮。长文完整显示，原图 contain；弹窗内可以另放真实原文 a 链接并标记新标签页。 不伪造笔记地址、作者和互动数据；没有目标内容或链接时明确显示缺口。页面内用 a 跳转，展开与弹窗用 button 或 details/summary；不能只给 div 加点击，也不能在整卡链接中嵌套按钮。键盘焦点清楚，触控目标建议至少 44px；图标旁有说明文字。打开方式与文字提示一致，内容和图表不因导航样式而删改。
手机端弹窗距视口两侧至少 12px，宽度自适应，正文 16px；内容内部滚动，关闭按钮不随正文移出可视区。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J04 就地展开预览 · 点击与跳转

【任务】请按以下「J04 就地展开预览」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：在卡片下展开摘要或完整补充内容。
需要材料：入口标题与封面、目标详情内容或真实原文 URL、所需打开方式；来源信息仅使用已提供材料。
使用原生 details/summary，summary 明确“展开笔记摘要”，保持原生展开状态和键盘行为；内容在原位置自然撑高。必要的标题与来源始终可见，长文不设置固定高度裁切；详细正文或原文入口用独立链接。不要用折叠藏关键限制，不让多个不相关折叠强制互斥。 不伪造笔记地址、作者和互动数据；没有目标内容或链接时明确显示缺口。页面内用 a 跳转，展开与弹窗用 button 或 details/summary；不能只给 div 加点击，也不能在整卡链接中嵌套按钮。键盘焦点清楚，触控目标建议至少 44px；图标旁有说明文字。打开方式与文字提示一致，内容和图表不因导航样式而删改。
手机端按阅读顺序自然展开，长文本换行，正文不小于 16px；内容增加时自然增高。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## D01 常驻分区目录 · 导航与目录

【任务】请按以下「D01 常驻分区目录」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：长页面随时定位章节，手机用紧凑目录。
需要材料：已有章节标题、唯一 ID、原正文与原有导航行为。
保留页面原有章节；桌面用 160–180px 目录侧栏与正文分列，position:sticky，顶部留出主导航高度。手机改成带名称的 select 或可折叠目录，不占满屏幕。目录项使用真实锚点，当前章节用 aria-current="location" 和文字/底色共同提示；点击后目标可见并接收焦点，支持片段链接和前进后退。缩放后重新计算导航高度，正文不被覆盖。
窄屏目录控件至少 44px 触控高度，长标题换行；正文独立占满可用宽度。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## D02 层级面包屑 · 导航与目录

【任务】请按以下「D02 层级面包屑」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：说明当前位置，逐级返回可用上级。
需要材料：真实页面层级、每级名称和可用目标链接。
使用 nav aria-label="当前位置" 包裹有序列表，分隔符用 CSS 或 aria-hidden，不作为链接名。仅确实存在的上级有真实链接；当前页用纯文字与 aria-current="page"。字号 14px，单个链接至少 44px 触控区域；路径来自真实信息层级，不为美观凭空造一级。与正文标题分开，保留足够留白。
窄屏允许自然换行；不把唯一返回入口藏进横向溢出。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## D03 返回顶部与阅读定位 · 导航与目录

【任务】请按以下「D03 返回顶部与阅读定位」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：读完长段内容后快速回到目录。
需要材料：真实的顶部或目录 ID、需要保留的页面状态。
使用带清楚文案的 a href="#真实顶部ID" 或等效按钮；入口可放在章节末尾或视口边角，但不能盖住文字、图表和系统安全区。目标有 tabindex="-1"，点击后滚动并移入焦点；尊重 prefers-reduced-motion。回到目录不清空当前搜索、收藏或页面状态，不能强制刷新。
触控区域至少 44px；固定入口离底部保留安全区，不遮挡底部按钮。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M01 空收藏引导 · 空白与缺失状态

【任务】请按以下「M01 空收藏引导」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：第一次使用时，告诉读者如何开始。
需要材料：真实收藏状态、可访问的浏览入口。
使用浅底、细边框与适量留白，标题 20px/700，说明 16px/1.75；明确写“还没有收藏”，提供唯一清楚的“浏览样式”入口。空收藏是用户状态，不是错误；不要伪造已收藏条目、默认替用户选择或自动收藏。真实收藏后显示对应条目和数量，取消最后一项后回到空状态。
手机上下排列，说明完整显示；主按钮触控高度至少 44px。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M02 无数据与缺字段 · 空白与缺失状态

【任务】请按以下「M02 无数据与缺字段」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：保留口径和待补材料，不把缺失画成零。
需要材料：已有标题和口径、确实缺少的字段、可选真实数据入口。
在原图表或指标位置显示“暂无可用数据”以及缺少的字段、时期、单位或来源；缺失、真实零、不适用分别标注。保留标题、口径和已有证据，不绘制假柱、虚假趋势或默认百分比。只有真实重新加载入口存在时才给重试按钮；未连接数据源时显示需要补充的材料清单。
文字自然撑高；必要来源和字段清单可直接阅读，不只放在悬停提示。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M03 无搜索结果与清除筛选 · 空白与缺失状态

【任务】请按以下「M03 无搜索结果与清除筛选」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：解释为什么没找到，并提供可恢复的下一步。
需要材料：真实搜索结果、当前筛选条件、可重置的字段。
保留当前关键词、类型和分组筛选，显示真实结果数 0；用普通说明色提示“暂时没有匹配项”，提供“清除筛选”按钮。点击后清空明确标示的筛选、展示实际可用结果并更新可读状态提示，不修改收藏、数据或后台查询口径。不要捏造推荐结果或把网络失败说成零结果。
按钮与说明上下排列，触控至少 44px；搜索框不被键盘焦点和顶部导航遮住。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M04 缺失原文链接 · 空白与缺失状态

【任务】请按以下「M04 缺失原文链接」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：有内容但缺少地址时，明确保留缺口。
需要材料：已有内容、来源、真实原文 URL 的可用状态。
保留已提供的封面、完整标题和来源，在原文入口显示“原文链接待提供”。没有 URL 时使用普通文本，不生成空 a、href="#"、伪造平台地址或假按钮；可用 details 解释需要补充的材料。获得经确认的 HTTPS 地址后才转为真实链接，若新标签页打开则明确提示并设 rel="noopener noreferrer"。
缺失说明完整可读，不只通过灰色区分；后续真实链接触控区域至少 44px。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## R01 长标题自然换行 · 手机与长内容

【任务】请按以下「R01 长标题自然换行」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：标题和正文增加时，页面仍然完整。
需要材料：真实完整长标题、正文、字号偏好与容器宽度。
给标题和正文设置 min-width:0、overflow-wrap:anywhere，避免固定高度、line-clamp、文字省略或强制 nowrap。桌面标题建议 32–40px/1.3，手机可 24–32px/1.4；字号以用户明确指定的分层参数为准。用足够行距与段距解决长度变化，原题全文保留。正文不小于 16px，不为了装下内容缩小正文。
先确保 320px 可完整阅读，再检查宽屏留白；中英文、数字和无空格长词都能换行。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## R02 长表格局部滚动 · 手机与长内容

【任务】请按以下「R02 长表格局部滚动」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：列多时保留全部字段，只在表格内部横向查看。
需要材料：完整真实表格、字段定义、单位、时间、来源和缺失说明。
使用语义化 table/caption/thead/tbody 与 scope；包在 max-width:100%、overflow-x:auto 的独立容器，容器有 role="region"、清楚 aria-label 和 tabindex="0"。表格按字段需要设置最小宽度，原列完整保留；首列可 sticky left:0，宽度不超过当前容器 40%，有实色底。就近提示“表格可横向查看”，键盘可滚动。长文字自然换行，数据、单位、来源、零与缺失口径不变。
320px 时只有表格内部可横向滚动，整页不溢出；不隐藏关键列或把列错配成行。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W01 步骤流程图 · 流程图

【任务】请按以下「W01 步骤流程图」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：讲清先做什么、后做什么，以及交付什么。
需要材料：任务目的、开始/结束条件、步骤 ID/名称/输入/动作/输出、顺序关系、已有判断与人工确认点、来源。
以单一主流程为骨架，开始/结束用胶囊节点，操作用圆角矩形，存在真实判断时才加入菱形和分支标签。每个操作用动词短标题，正文写输入、动作与输出；先后顺序有明确箭头。桌面短流程可从左到右，长流程从上到下或拆成带回链的子流程。节点标题 16–18px、说明 14–16px、内边距 16–20px、节点间距至少 28px；正文底色保持白或浅底，强调当前步骤而不是每一步都高亮。人工确认、返工和结束位置按材料明确标出，不把待审节点画成已通过。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端保留完整节点和关系，优先转为纵向步骤或按阶段分组；复杂图可在带名称、tabindex=0 的独立容器里平移/缩放，并附完整文字关系表。整页不得横向溢出，不通过缩小正文字号隐藏问题；打印保留全部节点、连线条件与来源。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W02 条件分支与决策树 · 流程图

【任务】请按以下「W02 条件分支与决策树」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把不同条件下的去向与行动区分开。
需要材料：入口问题、判断条件及其依据/阈值/单位、每个条件的去向、异常或未知情况、最终行动/停止点。
从一个入口问题开始，判断节点用菱形或明确“判断”标签；每条出线标注实际条件，如“满足条件”“不满足条件”，末端给出相应行动或停止点。多分支必须检查是否互斥及覆盖完整；无法确定的情形单独写“待确认”，不能默认归入“否”。保留阈值、单位、适用范围、依据与例外，不编造概率、权重、门槛或自动审批逻辑。复杂树按层级折叠分支时，提供展开全部和打印展开；收起仅影响展示。图中长条件以可读短标签配完整说明，箭头不得穿过条件文字。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端保留完整节点和关系，优先转为纵向步骤或按阶段分组；复杂图可在带名称、tabindex=0 的独立容器里平移/缩放，并附完整文字关系表。整页不得横向溢出，不通过缩小正文字号隐藏问题；打印保留全部节点、连线条件与来源。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W03 泳道协作流程 · 流程图

【任务】请按以下「W03 泳道协作流程」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：把步骤、责任与交接放到同一张图上。
需要材料：角色/团队清单、步骤 ID 与责任归属、步骤先后、交接内容与对象、审批/返工规则；未确认的责任标待确认。
每条泳道代表一个已提供的角色、团队或系统；道头明确写责任主体。任务节点只放在其实际负责的泳道内，箭头连接真实前后步骤；跨泳道交接标注交付物及接收方。共同负责时保留主责/协作关系，不凭空分派人员，不把泳道顺序当成部门等级。画布采用淡色分区、清楚道头和白底节点，交接线区别于普通顺序线并配图例。泳道的横向位置或节点距离不代表工期，除非另外提供真实时间刻度。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端优先按执行顺序排列，保留每步角色标签及交接对象；复杂泳道在独立区域横向滚动，道头可保持可见，同时给完整责任/交接表，不漏掉并行步骤。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W04 闭环迭代流程 · 流程图

【任务】请按以下「W04 闭环迭代流程」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：展示反馈如何进入下一轮，以及何时退出。
需要材料：真实迭代阶段、反馈材料及接收节点、进入下一轮的条件、退出标准、人工确认点和已知轮次（可缺）。
用环形或回折路径表达真实的迭代：每个阶段包含输入、动作和输出，回流箭头写清反馈内容与回到哪个节点，额外画出达到退出条件后的完成出口。未满足条件、缺证据或需人工确认时明确停留/返工位置；不能画成永远循环，也不能默认只迭代一次。环形面积、角度、箭头粗细仅表示路径，不表示占比或强度；没有量化数据不添加百分比、成功率、轮次数。节点较多时采用纵向主链加旁侧回流，避免为了圆形对称而删步骤。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端保留完整节点和关系，优先转为纵向步骤或按阶段分组；复杂图可在带名称、tabindex=0 的独立容器里平移/缩放，并附完整文字关系表。整页不得横向溢出，不通过缩小正文字号隐藏问题；打印保留全部节点、连线条件与来源。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W05 阶段 Roadmap · Roadmap

【任务】请按以下「W05 阶段 Roadmap」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：按阶段交代目标、行动、交付物和进入条件。
需要材料：项目目标、阶段及顺序、各阶段目标/行动/交付物/验收或进入条件、已有负责人/时间窗口/状态、关键依赖。
以已提供的阶段顺序组织路线图，每阶段用独立卡片或时间带，列出阶段目标、关键行动、交付物、负责人、进入下一阶段的条件和已有时间窗口；数量按内容决定，不强制三阶段。阶段标题 20–24px，正文 16px/1.75，交付物与关键门槛用少量重点色或浅底突出。没有可靠日期时使用“阶段一/阶段二”等顺序，并明确“阶段示意，宽度不代表时长”；不要编造月份或承诺截止日。若有真实日期，刻度和跨度对应日期并区分计划与实际；状态仅按已给记录显示。支持当前阶段指示和展开交付物，但不自动推进或替用户确认完成。路线图表达推进安排，不自动证明因果或商业结果。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端保留完整节点和关系，优先转为纵向步骤或按阶段分组；复杂图可在带名称、tabindex=0 的独立容器里平移/缩放，并附完整文字关系表。整页不得横向溢出，不通过缩小正文字号隐藏问题；打印保留全部节点、连线条件与来源。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W06 多线 Roadmap · Roadmap

【任务】请按以下「W06 多线 Roadmap」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：同一阶段下对齐多条工作线和依赖。
需要材料：工作线清单、共同阶段或真实时间窗口、每项行动/交付物/所属工作线/状态、确认的跨线依赖及依据。
采用“工作线 × 阶段或时间窗口”矩阵：行是材料中的工作线，列是共同阶段或真实时间刻度，卡片写行动与交付物；保留空单元格的真实含义，缺信息写“待确认”，不自动补满。多条线在同一列不代表互相依赖；只有明确提供的依赖才连线，并标注依赖类型或所需交付物。依赖过密时用编号引用和完整依赖表，避免箭头穿越正文。阶段列默认等宽且注明宽度不代表工期；有日期才绘制真实时间跨度。区分已承诺事项、候选计划和未知事项，不凭空加负责人、完成率或期限。行头浅底、内容白卡、重要里程碑细线或菱形点；颜色配合文字区分工作线和状态。先核对材料中的节点、关系及顺序，再排版；每个节点有唯一 ID，连线引用起点/终点 ID，并注明其表示执行顺序、条件分支、交接或依赖，不能把不同含义混成同一种箭头。正式节点数量由材料决定，不照搬预览。缺少关系、日期、责任人、状态或判断标准时写“待确认”，不代填为已确定。节点完整文字用 HTML 或可访问的 SVG 文本呈现，提供等价文字顺序或关系表；不要把流程变成不可编辑的整张图片。连接线在节点之间行走，有清楚箭头，不压住标签；尺寸或字体变化后重新计算连线。当前/完成/待确认等状态用文字配合颜色，不用颜色代替状态。
手机端按阶段分组列出各条工作线，每张任务卡保留工作线与阶段标签，跨线依赖用节点编号和关系表完整保留；需要矩阵对照时仅矩阵容器横向滚动，整页不溢出。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## P04 灵感策展组合 · 整页组合

【任务】请按以下「P04 灵感策展组合」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：专题导读 → 视觉展签 → 证据细读。
需要材料：专题正文、已授权图片、材料摘要、来源和真实链接。
以专题导读开篇，下面用完整图片与摘要带展示核心案例，再用页签档案卡排列延伸材料，末尾提供真实来源索引。桌面最大 1120px，图片和摘要不互相覆盖；没有图片时保留文字材料。图表只在真实字段支持时加入，不为装饰填入数值。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
组合中的布局定义：
专题索引展签：顶部显示专题名和完整导读，下方使用 3 列展签卡片：小编号、标题、简短摘要、真实章节锚点。最大宽 1120px，卡片间距 20px，卡片内边距 24px；可按文字或图片材料改变卡片高度，不能用省略号替代完整标题。后续章节按与目录一致的顺序展示。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
全图摘要带：用 figure 放完整主图，图下接浅底 figcaption 摘要带。摘要带桌面分为标题 1fr 与说明 2fr，内边距 24px；来源另起 14px 文本行。图片不叠字、不遮挡，若是截图应可查看原尺寸。没有图片时省略主图区域，按正常文字模块呈现。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
页签档案卡：每张卡片由小分类页签和正文面板组成，页签正常文档流贴近面板顶部，圆角 10px；面板使用细边框与 24px 内边距。桌面 2–3 列，卡片随内容增高；分类标签与标题都保留真实文字，不把纯装饰页签做成假按钮。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## P05 方法手册组合 · 整页组合

【任务】请按以下「P05 方法手册组合」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：章头导读 → 编号方法 → 资料回查。
需要材料：方法说明、输入输出、确认的步骤条件、来源材料。
采用章节色带组织不同主题；正文以编号清单表达已有步骤、条件与解释，证据条目台账放在相关章节之后。章节数和步骤数由材料决定，流程次序不构成因果结论。每项有输入、动作或说明、输出与已确认边界，未知项明确待确认。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。
组合中的布局定义：
章节色带：每章用整行浅色带作为标题区，左侧小型章节号，右侧 H2 和一句已有导语。章头内边距 24–32px，章节之间 56px，正文白底最大 760px；必要图表可扩展到 1120px。色带按当前色板装饰渐变，正文与图表的颜色语义保持独立。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
编号索引清单：使用 ol，每项按 56px 编号列和 minmax(0,1fr) 内容列排列。顺序号 32px，标题 20px，正文 16px，项间 24px。序号表示阅读顺序；没有真实排名时不使用 TOP 或奖牌。可加真实锚点，按钮只出现于实际可执行动作。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。
证据条目台账：使用语义列表或 table 展示来源条目；每项含材料名、摘要、证据状态、来源入口。标题 18px，说明 16px，状态文字 14px；行间只保留细线。颜色只辅助状态标签，缺来源直接注明。若含表格必须保留表头、完整行列与局部滚动，不把条目数当去重人数。 手机改为单列，保持 DOM 阅读顺序；正文不小于 16px，长标题和段落自然增高，不截断全文。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W07 交错里程碑 · Roadmap

【任务】请按以下「W07 交错里程碑」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：阶段卡片沿中轴交错排布。
需要材料：真实阶段顺序、交付物、验收门槛、可选真实日期。
使用有序列表承载阶段，桌面以细中轴连接上下错开的里程碑卡，卡片宽 40%–44%，卡片中写阶段名、交付物和确认条件。顺序用编号和文字表达；线条不穿文字，卡片自然增高。没有真实日期时只显示阶段编号，间距和卡片宽度不表示时长；有排期比较需求时用真实时间刻度。
手机将中轴靠左、所有卡片排右侧单列；阶段顺序、日期和条件完整保留。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## W08 交付关卡流程 · 流程图

【任务】请按以下「W08 交付关卡流程」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：输入、处理、检查关卡与交付逐层展开。
需要材料：输入材料、处理动作、真实检查条件、通过与退回路径、交付物。
用“输入 → 处理 → 检查 → 交付”的语义链展示真实流程。每个阶段为独立卡片，检查卡明确通过条件、未满足时的去向与负责角色；门槛未知直接写待确认。节点间连线按确认关系，不自动连成转化漏斗；复杂分支另列关系表。每卡 20–24px 内边距、16px 正文，颜色配文字区分阶段。
手机按有序列表呈现，检查未通过的去向以目标编号说明，不让回线穿过文字。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N07 艺廊留白封面 · 笔记封面

【任务】请按以下「N07 艺廊留白封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：完整画面被留白包围，标题像一张展签。
需要材料：已授权图片、完整标题、说明、来源和链接。
采用 3:4 视觉框架，内侧留白 24px，图片 object-fit:contain 完整呈现；图片下放完整标题、短说明和真实来源，如展签。标题 24–30px，正文 16px，内容过长时卡片增高。原图文字与证据不可裁掉；整卡使用真实链接，缺链接时保持静态。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## N08 索引票签封面 · 笔记封面

【任务】请按以下「N08 索引票签封面」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：竖向窄标签与横向大标题组合。
需要材料：栏目名称、顺序号或分类、标题、摘要、图片和真实链接。
左侧 48–64px 窄栏写栏目缩写或顺序号，右侧以 28–36px 完整标题为主体，下面显示图片或摘要；窄栏与正文间用一条细线。只将很短的栏目标签竖排，中文正文保持横排。编号不作为排名；标题不限制行数。以真实 a 元素承接原文。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J05 上一篇与下一篇 · 点击与跳转

【任务】请按以下「J05 上一篇与下一篇」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：连续阅读时，直接前后切换。
需要材料：真实有序文章清单、标题、URL 或站内内容。
底部两张等宽导航卡，分别用“上一篇”“下一篇”与实际标题指向真实目标。没有上一项或下一项时显示边界文字，禁用不存在的动作；不要循环到不相关内容。链接保留键盘焦点，按钮切换站内示意时使用 aria-live 提示当前标题；切换不影响收藏与全站样式。
手机两张卡可纵向排列，完整标题换行，触控高度至少 44px。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## J06 目录旁读 · 点击与跳转

【任务】请按以下「J06 目录旁读」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：点左边的条目，在右边读完整摘要。
需要材料：若干真实材料的编号、完整标题、正文或摘要及来源。
使用两列：左侧目录 30%，右侧详情 70%，间距 20px。目录通过 button 选择条目，aria-pressed 表示当前项；右侧 region 使用已有标题、摘要和来源，aria-live=polite 通知变化。不伪造新页面 URL；若有完整原文则另外放真实链接。
窄屏目录横向换行，详情在下；按钮不压成省略标题，不陷入多层内部滚动。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B06 分组表头对照 · 表格

【任务】请按以下「B06 分组表头对照」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：上层按主题分组，下层保留具体字段。
需要材料：真实行列、分组与字段映射、单位、时期、分母和来源。
使用两层 thead：第一层 th scope=colgroup 与正确 colspan 对应字段分组，第二层 th scope=col；每行首列 th scope=row。同组用浅底或细分隔线，正文 16px、内边距 12px 16px，数值右对齐。复杂对应补充 id/headers，不能合并不相同口径的字段。
窄屏仅表格容器横向滚动，保留两层表头和全部字段；容器可键盘聚焦。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## B07 资料清单表 · 表格

【任务】请按以下「B07 资料清单表」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：材料名、用途、来源，逐条容易回查。
需要材料：真实资料清单、用途摘要、来源和已核实状态。
语义表格保留“材料／说明／来源”等真实字段，材料名半粗，说明自然换行，来源为真实链接或明确待提供。行距 1.7，单元格 14px 16px 内边距，行间细线，不使用交替颜色暗示优劣。可加文字状态标签但不得虚构审阅状态。
手机保留完整表头并局部滚动；若改为卡片，每一字段都重复显示标签，不错配值。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E05 观点证据边界三联 · 结论与证据

【任务】请按以下「E05 观点证据边界三联」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：同一判断的依据与适用范围并排可读。
需要材料：一句已有判断、可回查的支撑材料、时间口径与适用边界。
三个并列面板依次写“观点／证据／边界”，桌面比例 1:1.2:1，间距 16px，内边距 20–24px。每格使用真实 H3、正文和来源；证据缺失写待补证，不能自动把观点标为事实。观点与证据对应，边界紧邻展示，手机按同样顺序堆叠。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## E06 批注式引文 · 结论与证据

【任务】请按以下「E06 批注式引文」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：原话完整保留，分析紧贴但单独成栏。
需要材料：真实原文、出处、必要上下文和已有分析。
采用 blockquote 与 cite 标出完整提供的原文及真实出处；旁边用浅底批注区写分析，明确“解读”或“假设”。桌面 1.4:1，手机原文在前、批注在后。引号与边线只作装饰，不把删节、转述伪装成原话；较长引文使用 16px 正文，不整段放大。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## D04 胶囊章节导航 · 导航与目录

【任务】请按以下「D04 胶囊章节导航」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：轻量胶囊串起文章章节。
需要材料：页面实际章节标题、锚点、导航高度。
nav 内用真实锚点链接形成可换行胶囊：字体 14–16px、触控高度至少 44px、圆角 999px、浅底和细线。当前章节用 aria-current=location 和文字/边线共同区分；正确维护滚动位置和可见焦点。目标标题 scroll-margin-top 为实际导航高度加 16px，不覆盖标题。
手机胶囊换行，不挤成一条超宽横栏；导航太高时改为可展开目录，不遮住正文。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## D05 章节索引卡 · 导航与目录

【任务】请按以下「D05 章节索引卡」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：每张入口卡都说明下一节能看到什么。
需要材料：真实章节清单、说明与目标元素 ID。
在章节开头展示 2–3 列索引卡，每张用完整章节名、已有简短描述和真实锚点。卡片整块可点击，内部不嵌套按钮；图标和箭头标记 aria-hidden。点击后定位目标标题并保持键盘可继续阅读，浏览器返回行为正常。缺章节入口时移除链接而非跳到空白。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M05 局部加载失败 · 空白与缺失状态

【任务】请按以下「M05 局部加载失败」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：一处出错时保留其他可读内容。
需要材料：真实加载状态、模块名称、已实现的重试动作。
只在失败模块内显示简洁状态面板，写清失败对象和真实错误状态；正文其他部分保持可读。重试按钮仅在有真实请求逻辑时出现，等待中禁用且有状态提示，失败后允许再次操作；不能用定时成功伪装网络恢复。本册重试必须标明交互示意。
提示与按钮换行，错误详情可展开；不因一处失败覆盖整页或清空已有内容。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## M06 部分材料待补 · 空白与缺失状态

【任务】请按以下「M06 部分材料待补」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：已有材料可继续阅读，缺口单独列明。
需要材料：已提供材料、缺失字段、对判断的影响、已知补充方式。
将已提供内容与待补字段分区呈现；缺口区使用浅底列表，逐项写字段名、缺失原因或当前未知、影响哪个判断。未知不补零，不显示虚构完成率。可用原生 details 展开补充清单，但关键缺失提示始终可见。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## R03 分栏自适应堆叠 · 手机与长内容

【任务】请按以下「R03 分栏自适应堆叠」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：宽屏图文并置，窄屏按阅读顺序展开。
需要材料：实际图像、完整正文、来源和容器约束。
桌面用 minmax(0,1.2fr) minmax(0,1fr) 两列网格，间距 24–32px；父容器不足 640px 时改为单列。推荐容器查询，提供媒体查询回退。DOM 固定为主视觉、标题正文、来源；不靠 CSS order 打乱键盘或读屏顺序，图片 max-width:100%。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## R04 摘要与完整阅读 · 手机与长内容

【任务】请按以下「R04 摘要与完整阅读」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
用途：先看摘要，再就地展开补充全文。
需要材料：确认过的摘要、完整正文、始终可见的来源和关键边界。
先显示独立撰写且经过确认的摘要，再用原生 details/summary 提供“阅读全文”入口。全文在 DOM 中完整保留，展开后自然增高；必要结论、来源和限制不能只放折叠区域。摘要不是用 line-clamp 截取正文，也不能替代原意。
手机采用单列和自然高度，保留完整文字、来源与控制；不因内容变多缩字或截断。
四层文字数值是整页基础；组件内有具体字号建议时先用组件建议，标有用户自定的对应字段优先。字体与实际字色使用下方方案。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## F01 清晰黑体 · 字体

【任务】请按以下「清晰黑体字体搭配」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
标题与正文使用系统无衬线字体，数字采用等宽数字特性。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## F02 宋体标题 · 字体

【任务】请按以下「宋体标题字体搭配」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
标题使用宋体或系统衬线回退，正文维持清晰黑体。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：宋体标题。大标题、小标题、正文、注释分别设置。
大标题（H1）：系统宋体；font-family: 'Songti SC', 'STSong', 'Noto Serif CJK SC', ui-serif, serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：系统宋体；font-family: 'Songti SC', 'STSong', 'Noto Serif CJK SC', ui-serif, serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## F03 数字等宽 · 字体

【任务】请按以下「数字等宽字体搭配」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
中文维持黑体，数字与编码使用等宽字体，方便比较与对齐。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：数字等宽。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 ui-monospace, 'SFMono-Regular', Menlo, Consolas, monospace，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## F04 胡晓波青年宋 · 字体

【任务】请按以下「胡晓波青年宋字体搭配」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
大标题和小标题使用胡晓波青年宋粗款，正文和注释保留清晰黑体。准确家族名为 HuXiaoBo-qingniansong；未安装时按给定顺序回退。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：胡晓波青年宋。大标题、小标题、正文、注释分别设置。
大标题（H1）：胡晓波青年宋；font-family: 'HuXiaoBo-qingniansong', '胡晓波青年宋', 'Songti SC', 'STSong', serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：胡晓波青年宋；font-family: 'HuXiaoBo-qingniansong', '胡晓波青年宋', 'Songti SC', 'STSong', serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## F05 Freeland 英文标题 · 字体

【任务】请按以下「Freeland 英文标题字体搭配」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
大标题的拉丁字母使用 Freeland 手写字体，中文回退为宋体；小标题用宋体，长段正文和注释用清晰黑体。Freeland 保持原生 400 字重，不人为加粗；未安装时使用回退字体。
页面底 #fafbfc、内容白底 #ffffff、分隔线 #e8ebf0；装饰强调色 #ec407a，辅助 #4b91df；浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)，更浅底 #fff7fb → #eef7ff。
文字方案：Freeland 英文标题。大标题、小标题、正文、注释分别设置。
大标题（H1）：Freeland；font-family: 'Freeland', 'Songti SC', 'STSong', serif；字号 40px，字重 400，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：系统宋体；font-family: 'Songti SC', 'STSong', 'Noto Serif CJK SC', ui-serif, serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V01 正文字色 · 文字颜色

【任务】请按以下「正文字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「正文字色」：实际值 #273445，字重 400。连续阅读的完整段落，保持清晰。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V02 大标题字色 · 文字颜色

【任务】请按以下「大标题字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「大标题字色」：实际值 #ad1457，字重 700。页面大标题 H1，明确主题；与小标题独立设置。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V03 注释字色 · 文字颜色

【任务】请按以下「注释字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「注释字色」：实际值 #3b6d98，字重 400。图注、来源、脚注和补充解释，字号和对比度仍须可读。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V04 重点字色 · 文字颜色

【任务】请按以下「重点字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「重点字色」：实际值 #bd3362，字重 500。少量关键词用颜色强调，颜色不替代论据。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V05 加粗字色 · 文字颜色

【任务】请按以下「加粗字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「加粗字色」：实际值 #ad1457，字重 700。用 strong 标出重要语句；加粗与换色可以独立使用。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V06 Highlight 底色 · 文字颜色

【任务】请按以下「Highlight 底色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「Highlight 底色」：实际值 #fde8ef，字重 400。短词句的标记背景，配合独立的高亮字色。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V07 Highlight 字色 · 文字颜色

【任务】请按以下「Highlight 字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「Highlight 字色」：实际值 #ad1457，字重 400。mark 内的文字，须与高亮底色搭配可读。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V08 链接字色 · 文字颜色

【任务】请按以下「链接字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「链接字色」：实际值 #386ba5，字重 400。实际可点击的链接，保留下划线和键盘焦点。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。

## V09 小标题字色 · 文字颜色

【任务】请按以下「小标题字色」为我制作或修改 HTML。下面已给出具体规则，无需访问收藏册、查编号或读取之前的聊天。
【输入】使用我随消息提供的正文、真实数据、图片或现有 HTML。若没有提供内容，先列出最少需要补充的材料；不得把样式示意文案、演示值当作业务内容。已有 HTML 优先保留其内容结构和交互。

【样式与实施要求】
仅调整「小标题字色」：实际值 #3b6d98，字重 700。章节与模块小标题 H2/H3，区分正文和页面大标题。
为兼容当前背景与字体，参考以下完整方案；未指定的其他角色沿用目标页面。
配色采用「C01 谷雨粉蓝」。装饰强调色 #ec407a，辅助色 #4b91df；页面底色 #fafbfc，内容面板 #ffffff，分隔线 #e8ebf0。浅底渐变 linear-gradient(135deg, #fce4ec, #e3f2fd)；更浅的辅助底 linear-gradient(135deg, #fff7fb, #eef7ff)；装饰细线 linear-gradient(90deg, #db4c78, #a58a55)。粉蓝渐变用于封面与主视觉浅底，粉色强调重点，柔和蓝用于解释；粉金渐变只作细线。
文字方案：清晰黑体。大标题、小标题、正文、注释分别设置。
大标题（H1）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 40px，字重 700，行高 1.25，段后距 24px，字色 #ad1457。未单独自定，数值为整页基础。页面主题；手机最大 32px，长标题自然换行。
小标题（H2 / H3）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 24px，字重 700，行高 1.4，段后距 16px，字色 #3b6d98。未单独自定，数值为整页基础。章节和模块标题；手机最大 26px，必要时再区分 H2 与 H3。
正文（P）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 16px，字重 400，行高 1.75，段后距 18px，字色 #273445。未单独自定，数值为整页基础。连续阅读；内容增加时自然增高，不缩小字号硬塞。
注释（图注 / 来源）：清晰黑体；font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif；字号 14px，字重 400，行高 1.65，段后距 12px，字色 #3b6d98。未单独自定，数值为整页基础。来源、单位和必要边界；不能因为字号小而省略。
H3 次级小标题建议 20px；组件已有明确层级时保留其层级。
数字字体 inherit，font-variant-numeric:tabular-nums。
其他文字角色：重点字色 #bd3362 / 字重 500；加粗字色 #ad1457 / 字重 700；Highlight 底色 #fde8ef / 字重 400；Highlight 字色 #ad1457 / 字重 400；链接字色 #386ba5 / 字重 400。重要语句用 strong（700），高亮用 mark，链接保留下划线和可见焦点。以上为实际使用色；已检查白底、当前浅底和 Highlight 底的可读性，换背景后重新检查。
胡晓波青年宋的准确家族名为 HuXiaoBo-qingniansong，使用细款 300 或粗款 700；Freeland 用于英文标题，保持 400，不人为加粗，中文按给定宋体序列回退。字体依赖接收设备已安装字体；此方案不附带或下载字体文件，未安装时使用回退字体。只调整文字展示，不改正文、来源和图表数据或颜色含义。

【内容与图表边界】完整保留正文、已有结论的证据等级、数据、单位、口径、分母、时间、来源和已有图表交互。章节数、模块数、文字长度按内容决定，不为套样式删证据或缩小正文。图型由问题和数据选择，收藏不是图型白名单；字段不足时说明缺口，不补造数字、目标、阈值、评分、引语、关系或结论。页面装饰色不能覆盖已有图表的系列含义、坐标轴、图例、visualMap 或事件。
【交付】提供可直接打开的完整 HTML；CSS 限定在本次报告根容器和专用类名内，不污染外部页面。优先单文件内嵌样式与必要脚本；若目标已有工程或依赖约定，遵守其约定并说明依赖。只有输入不足时才保留清楚注明缺口的空位。
【验收】检查桌面与 320–390px 手机：正文完整、层级清楚、无整页横向溢出；表格可独立滚动，图表在显示和缩放后适配；导航、键盘焦点、链接和交互可用，打印不丢必要内容。正文与高亮底色对比清楚；实际打开检查，无相关控制台错误。
